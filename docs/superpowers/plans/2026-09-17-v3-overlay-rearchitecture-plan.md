# V3 Overlay Rearchitecture — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the WPF layered-window overlay renderer with a GPU-composited renderer (Direct3D11 + Direct2D + DirectWrite + DirectComposition) matching the fluidity of Kapps/GoFast, while preserving every validated feature of the current app (telemetry logic, widget behavior, configuration options). The current app is frozen as a backup; the new app is built alongside it for side-by-side comparison.

**Folder isolation (user directive, 2026-09-18):** All V3 code lives under the top-level `v3/` folder (`v3/src/IracingLiveCoach.OverlayHost`, and every subsequent V3 project). **Nothing under `src/` (V2's `IracingLiveCoach.App`, `IracingLiveCoach.Core`) is modified, moved, or deleted for V3's sake.** Both trees stay alive and buildable side by side until the user explicitly authorizes consolidating onto one. Practical consequence for Phase 1 below: where the plan originally said "move `TelemetryReader.cs` into Core," it now means **copy** it into a new `v3/src/IracingLiveCoach.Core` project — a deliberate, acknowledged duplication, not an oversight, made to guarantee V2 never breaks. Reconciling the duplication (or not) is part of whatever the user decides at consolidation time.

**Architecture:** A new `v3/src/IracingLiveCoach.Core` project holds V3's own copy of UI-framework-agnostic logic (telemetry reading, domain models, calculations, capabilities, persistence) — copied from, not shared with, V2's `src/IracingLiveCoach.Core`. `v3/src/IracingLiveCoach.OverlayHost` owns a DirectComposition swapchain per overlay window and draws every widget through a shared declarative layout engine using Direct2D/DirectWrite. The Control Center (configuration UI) stays WPF, communicating with the overlay host over a local typed IPC channel so config changes apply live without restarting.

**Tech Stack:** C#. `IracingLiveCoach.OverlayHost` targets **net9.0-windows** (confirmed during Phase 0 research: the classic `Vortice.Direct2D1`/`Vortice.DXGI` family at 3.8.3 has no DirectWrite bindings at all — never published, verified against the amerkoleci/Vortice.Windows source tree — DirectWrite only exists in the actively maintained `Vortice.Win32.Graphics.*` family at 2.5.0, which requires net9.0/net10.0). `IracingLiveCoach.Core` and the V2 app stay on net8.0, unaffected — only the new OverlayHost project takes the newer TFM. .NET 9 SDK installed via winget on 2026-09-17, user-confirmed. Packages pinned to 2.5.0: `Vortice.Win32.Graphics.Direct3D11`, `Vortice.Win32.Graphics.Dxgi`, `Vortice.Win32.Graphics.Direct2D`, `Vortice.Win32.Graphics.DirectWrite`, `Vortice.Win32.Graphics.DirectComposition`. IRSDKSharper (existing telemetry library, unchanged), WPF (Control Center only, net8.0).

**Spec:** [docs/superpowers/specs/2026-09-17-v3-overlay-rearchitecture-spec.md](../specs/2026-09-17-v3-overlay-rearchitecture-spec.md) — this plan argues from that spec; read both.

## Global Constraints

(Copied verbatim from the spec — every task below implicitly inherits these.)

- Six widgets only: Standings, Relative, Weather Report, Fuel Calculator, Radar, Start Helper. No map, no inputs widget, no session summary, no separate delta widget.
- One shared visual/component system for all widgets — no per-widget bespoke layout code with hardcoded positions.
- Preview (Control Center) and real overlay render through the exact same layout/rendering engine — never two divergent visual implementations.
- No widget exceeds 25% of the target area's physical width; a 7-driver Relative/Standings never exceeds 35% of physical height, measured post-DPI-scale.
- No decorative widget titles or column-name headers during a race in Standings/Relative.
- Full first+last driver names by default; abbreviation is opt-in, never automatic.
- ΔiRating appears only in Standings, in the same badge as iRating, same row (`3.694 +12`), never in Relative.
- Class color strip keyed by session ClassId only — never manufacturer, licence, team, or row order.
- Overtake (OT) shows seconds + a proportional bar only — no PRONTO/ATIVO/RECARGA text, no icons, in the live cell. States (available/triggered/cooldown/depleted/unsupported/unknown) are modeled separately from the numeric balance.
- All colors come from the single normative HEX palette (spec §16), centralized as tokens — never scattered literals.
- Antialiasing is an acceptance requirement: DirectWrite grayscale AA on transparent surfaces, `D2D1_ANTIALIAS_MODE_PER_PRIMITIVE` for vector geometry, correct premultiplied-alpha handling, per-monitor DPI awareness with target/cache recreation on DPI or monitor change.
- Reference test hardware: Ryzen 5 5500X3D, GTX 1660 6GB, 16GB RAM, 1080p165Hz, iRacing borderless-windowed. No performance claims for other configurations without separate testing.
- Primary typeface is **Barlow Semi Condensed** (spec §5) — a deliberate change from V2's Satoshi. Rajdhani is opt-in for numeric fields only, and only if it doesn't distort proportions. No `scaleX`/`scaleY` letter-stretching anywhere; tabular numerals for aligned decimals.
- Reuse existing brand/flag/icon assets and resolution logic first (spec §18): `src/IracingLiveCoach.App/BrandIcons.cs`, `BrandImageLoader.cs`, `src/IracingLiveCoach.Core/CountryFlags.cs`, and `src/IracingLiveCoach.App/Assets/{Brands,Flags}`. Audit before building anything new — no parallel/incompatible catalog.
- The overlay host must survive device-lost, sim disconnect, session change, and monitor removal without a restart (spec §3) — this is a shipped requirement of Phase 1, not just an observation from Phase 0's spike.

---

## Scope Decomposition

This spec spans multiple independent subsystems (GPU rendering core, shared layout engine, six widgets, Control Center, persistence/profiles, performance validation). Per plan-writing practice, it is broken into phase-plans below; **only Phase 0 is detailed to the step level in this document**, because the spec itself mandates proving GPU viability before committing to the full rewrite (§3: *"Primeiro construa uma prova técnica... Confirme a viabilidade no Windows antes de expandir todos os widgets"*). Phases 1–7 are scoped with concrete files and acceptance criteria; each gets its own bite-sized task breakdown (a follow-up plan document) once the phase before it is validated and merged. Re-planning between phases is intentional, not a shortcut — a GPU spike commonly changes assumptions the later phases depend on.

| Phase | Deliverable | Plan status |
|---|---|---|
| 0 | GPU technical proof (transparency, click-through, text, DPI, multi-monitor, Radar/Start Helper pacing) | **Detailed below** |
| 1 | Core extraction + telemetry adapter + IPC skeleton | Scoped below, detailed plan written after Phase 0 signs off |
| 2 | Shared declarative layout engine + visual token system | Scoped below |
| 3 | Standings + Relative (incl. OT) | Scoped below |
| 4 | Weather, Fuel, Radar, Start Helper | Scoped below |
| 5 | Control Center V3 (all tabs, live preview) | Scoped below |
| 6 | Profiles, persistence, import/export, undo/restore | Scoped below |
| 7 | Validation, benchmarking, packaging | Scoped below |

---

## Phase 0 — GPU Technical Proof

**Files:**
- Create: `v3/src/IracingLiveCoach.OverlayHost/IracingLiveCoach.OverlayHost.csproj`
- Create: `v3/src/IracingLiveCoach.OverlayHost/GpuOverlayWindow.cs`
- Create: `v3/src/IracingLiveCoach.OverlayHost/DeviceResources.cs`
- Create: `v3/src/IracingLiveCoach.OverlayHost/Program.cs`
- Test: manual/visual (documented below) — no automated UI test framework exists for this; acceptance is evidence-based per spec §13/§19.

**Interfaces:**
- Produces: `DeviceResources` — owns `ID3D11Device`, `IDCompositionDevice`, `IDCompositionTarget`, `ID2D1DeviceContext`, `IDWriteFactory`, with `Resize(int width, int height)` and `HandleDeviceLost()` methods that later phases (1+) depend on for all drawing.
- Produces: `GpuOverlayWindow` — a Win32 window wrapper exposing `SetClickThrough(bool)`, `BeginDraw()/EndDraw()`, and a `Dpi` property, which Phase 2's layout engine will render into.

- [x] **Step 1: Confirm Vortice.Windows package versions** — DONE 2026-09-17

Verified against `api.nuget.org` and the `amerkoleci/Vortice.Windows` GitHub source tree: the classic family (`Vortice.Direct2D1`/`Vortice.DXGI`/`Vortice.Direct3D11`/`Vortice.DirectComposition`, latest 3.8.3, net8.0-compatible) has **no DirectWrite bindings in any published version** — confirmed by inspecting the actual repo source folders (only an `ArcSegment.cs`/geometry-style `Vortice.Direct2D1` project exists, no DirectWrite files) and by a 404 on `vortice.directwrite` on NuGet. DirectWrite bindings exist only in the newer, actively maintained `Vortice.Win32.Graphics.*` family (latest synced version **2.5.0**), which targets **net9.0/net10.0 only**. Decision (user-confirmed): install .NET 9 SDK and target the new `OverlayHost` project at `net9.0-windows`, using the 2.5.0 family for all five bindings — `Vortice.Win32.Graphics.Direct3D11`, `Vortice.Win32.Graphics.Dxgi`, `Vortice.Win32.Graphics.Direct2D`, `Vortice.Win32.Graphics.DirectWrite`, `Vortice.Win32.Graphics.DirectComposition`. `IracingLiveCoach.Core`/App/V2 remain net8.0, untouched. .NET 9 SDK (9.0.318) installed via `winget install --id Microsoft.DotNet.SDK.9` and confirmed with `dotnet --list-sdks`.

- [ ] **Step 2: Create the OverlayHost project**

```bash
dotnet new console -n IracingLiveCoach.OverlayHost -o v3/src/IracingLiveCoach.OverlayHost --framework net9.0-windows
dotnet sln add v3/src/IracingLiveCoach.OverlayHost/IracingLiveCoach.OverlayHost.csproj
```
Edit the `.csproj` to add `<UseWindowsForms>false</UseWindowsForms>`, `<OutputType>WinExe</OutputType>`, and the five pinned `Vortice.Win32.Graphics.*` package references (version 2.5.0) from Step 1.

- [ ] **Step 3: Win32 layered-but-GPU-composited window**

In `GpuOverlayWindow.cs`, create a borderless, topmost, `WS_EX_LAYERED | WS_EX_TRANSPARENT | WS_EX_TOOLWINDOW` window (transparent flag toggled by `SetClickThrough`), but back it with a DirectComposition visual tree instead of `UpdateLayeredWindow` — this is the actual fix for the software-compositing bottleneck. Use `DCompositionCreateDevice3`/`IDCompositionDevice.CreateTargetForHwnd` bound to the HWND, one `IDCompositionVisual` per widget window, committed via `IDCompositionDevice.Commit()`.

- [ ] **Step 4: Direct2D draw target + one static text draw**

In `DeviceResources.cs`, create the `ID3D11Device` (`D3D11CreateDevice` with `D3D11_CREATE_DEVICE_BGRA_SUPPORT`), wrap it for D2D via `CreateDXGIDeviceContext`, create an `IDXGISwapChain1` in `DXGI_SWAP_EFFECT_FLIP_SEQUENTIAL` composition mode, and get an `ID2D1DeviceContext`. Draw one static string ("V3 PROOF") with DirectWrite, grayscale AA, at a fixed position, and present via `IDCompositionDevice.Commit()`.

- [x] **Step 5: Verify real per-pixel transparency** — DONE 2026-09-17, CONFIRMED

Built and ran the proof app. Screenshot showed the drawn text and dot rendering directly over other on-screen windows with no black box and no layered-window softening — genuine DirectComposition compositing. Root cause of success: `WS_EX_NOREDIRECTIONBITMAP` + a composition swap chain (`CreateSwapChainForComposition`, `AlphaMode.Premultiplied`, `SwapEffect.FlipSequential`) bound into the visual tree via `IDCompositionVisual.SetContent`.

- [x] **Step 6: Click-through toggle** — DONE 2026-09-17, CONFIRMED (style-flag level)

`SetClickThrough`/`ApplyClickThrough` flip `WS_EX_TRANSPARENT` via `GetWindowLongW`/`SetWindowLongW`. Verified by focusing the window and sending SPACE remotely, then screenshotting: the on-screen label flipped from "ON" to "OFF" as expected. A real mouse-click-passes-through-to-iRacing test still needs a human at the mouse.

- [x] **Step 7: DPI + multi-monitor test** — DONE 2026-09-17, PARTIAL

Only the machine's actual current setting was tested: 96 DPI / 100% scale, single monitor — confirmed sharp text at that scale. 125%/150% and a second monitor were **not** tested (would have required changing the user's display settings or hardware not available in this session) — recorded as an open gap in the findings doc, not silently assumed to work.

- [x] **Step 8: Radar/Start Helper pacing test** — DONE 2026-09-17, CONFIRMED, exceeds target

A moving dot presented every vsync (`Present(1, ...)`) logged real frame times to `%AppData%\iracing-live-coach\v3-phase0-pacing.log`. Result: p50=6.06ms, p95=6.11ms, p99=6.13ms (≈165Hz, matching the reference monitor exactly), with only rare single-frame hitches (max spikes to ~6.5-14ms, one 35.5ms outlier over ~2.5 minutes) — no backlog, no growing queue.

- [x] **Step 9: Document findings** — DONE 2026-09-17

Full findings in [docs/superpowers/specs/2026-09-17-v3-phase0-findings.md](../specs/2026-09-17-v3-phase0-findings.md), including the exact Vortice.Win32.Graphics.* 2.5.0 packages used, all four step results above, and carried-forward gaps for Phase 1 (DPI-change/monitor-change recovery, actual 125/150% + multi-monitor testing, device-lost recovery — all deliberately out of scope for this spike per the plan).

**Gate result: transparency, click-through, and text sharpness all passed. Phase 1 is unblocked.**

- [ ] **Step 10: Commit**

```bash
git add v3/src/IracingLiveCoach.OverlayHost docs/superpowers/specs/2026-09-17-v3-phase0-findings.md IracingLiveCoach.sln
git commit -m "feat(v3): GPU overlay technical proof -- DirectComposition transparency, click-through, DPI, frame pacing

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
```

---

## Phase 1 — Core Extraction + Telemetry Adapter

**Status: mostly done (2026-09-18).** Remaining: the IPC skeleton (not yet started — needed once the Control Center exists in Phase 5, not blocking Phase 2/3 work) and a real forced device-removal test (see note below).

- [x] Create `v3/src/IracingLiveCoach.Core/IracingLiveCoach.Core.csproj` — targets net9.0 (no GPU dependency forcing `-windows`; matches OverlayHost's major version without needing the platform suffix).
- [x] Copy (not move — see Folder isolation note above) `src/IracingLiveCoach.Core/*.cs` (Models, LiveCoachEngine, BaselineSync, CountryFlags, LapTimeFormatting, WidgetLayoutStore) → `v3/src/IracingLiveCoach.Core/`. V2's originals untouched.
- [x] Copy (not move) `src/IracingLiveCoach.App/TelemetryReader.cs` → `v3/src/IracingLiveCoach.Core/Telemetry/TelemetryReader.cs`, namespace changed to `IracingLiveCoach.Core.Telemetry` (physical move within the copy, not a logic change) — the validated P2P dual-path logic (`ReadP2P`, `ReadP2PCount`, `P2PMaxSeconds = 200`) is byte-for-byte identical. Added three small ADDITIVE members only (no existing logic touched): `IsRaceSession`, `PlayerCarIdx` read-only pass-through properties, and `CaptureSnapshot()`. V2's original file untouched.
- [x] Create `v3/src/IracingLiveCoach.Core/Telemetry/TelemetrySnapshot.cs` — immutable record (`CapturedAtUtc`, `HasRecentTelemetry`, `SessionDetected`, `IsRaceSession`, `PlayerCarIdx`) plus a `NeverCaptured` static default, per spec §1.
- [x] Copy `tests/IracingLiveCoach.Core.Tests/*.cs` (all 6 files, 38 tests) → `v3/tests/IracingLiveCoach.Core.Tests/`, retargeted to net9.0 and referencing the v3 Core project.
- [x] `v3/src/IracingLiveCoach.OverlayHost/DeviceResources.cs` — refactored Phase 0's one-shot `Create()` into `Initialize()` (re-runnable) + `HandleDeviceLost()` (disposes every GPU-dependent COM object via `ReleaseGpuResources()`, then re-`Initialize()`s against the same HWND/size) + a `DeviceRecovered` event for future GPU-resident caches (fonts/icons, spec §18) to hook their own re-creation. `RenderFrame` now checks `EndDraw`/`Present`'s `HResult` for `DXGI_ERROR_DEVICE_REMOVED/_RESET/_HUNG` (hardcoded stable Win32 values — not exposed as named constants in this binding) and returns `false` (recovered, try again next frame) instead of throwing.
- [ ] `v3/src/IracingLiveCoach.OverlayHost/Ipc/` — typed, versioned local IPC between Control Center and OverlayHost. **Not started** — no Control Center exists yet to talk to (Phase 5); revisit when that phase starts.

**Acceptance — verified:**
- `v3/src/IracingLiveCoach.Core` builds standalone with zero WPF/UI references.
- V2's own `dotnet build IracingLiveCoach.sln` / `dotnet test` still succeed completely untouched — confirmed after every V3 change in this phase (proves copy-not-move never regressed V2).
- Both test projects pass: `IracingLiveCoach.Core.Tests.dll (net8.0)` — V2, 38/38 — and `IracingLiveCoach.Core.Tests.dll (net9.0)` — V3 copy, 38/38.
- Smoke test: built Release, launched the OverlayHost exe, confirmed it runs 3+ seconds without crashing after the `DeviceResources` refactor (transparency/click-through already re-verified in Phase 0, not re-screenshotted here).

**Acceptance — NOT verified, honestly flagged:** a real forced-device-removal test (unplugging/disabling the monitor, or calling `ID3D11Device.RemoveDevice`) was not performed. `ID3D11Device.RemoveDevice` is a DirectX 11.1 (`ID3D11Device1`+) method and this Vortice.Win32.Graphics.Direct3D11 2.5.0 binding does not expose `ID3D11Device1`/`RemoveDevice` at all (checked the generated interface files directly — absent). The recovery code follows the standard, MSDN-documented device-lost pattern (check `EndDraw`/`Present` HRESULT, tear down, rebuild), but it has not been exercised by an actual device-lost event. This needs either: a real GPU driver reset/monitor unplug during a later manual test pass, or a different mechanism to force `DXGI_ERROR_DEVICE_REMOVED` in code, found in a later phase.

## Phase 2 — Shared Declarative Layout Engine

**Status: done (2026-09-18), except the one manual/interactive item noted below.**

- [x] `v3/src/IracingLiveCoach.OverlayHost/Theme/PaletteTokens.cs` — every HEX value from spec §16 (both tables: surfaces/text/controls, and classes/licences/semantic states), each as a named `Color4` constant with the spec's own default alpha, plus the corner-radius/border-thickness constants from the same section. Nothing hardcoded elsewhere from this point forward (Phase 0's inline white/cyan proof-code colors predate this file and are Phase 3's job to replace when a real widget consumes them).
- [x] `v3/src/IracingLiveCoach.OverlayHost/Layout/ColumnDefinition.cs` — per-column config (key, Fixed/Flexible width mode, width, min width, alignment, padding, visibility, order, decimal places), pure data with zero GPU dependency, so it's identical for the Control Center's preview and the live overlay (spec §12).
- [x] `v3/src/IracingLiveCoach.OverlayHost/Layout/WidgetLayoutEngine.cs` — pure geometry math: `ComputePhysicalLimits` (spec §17's `floor(0.25×width)`/`floor(0.35×height)` ceilings) and `LayoutTable` (resolves column offsets, shrinks only Flexible columns toward their minimums when over budget, reports `FitsWithinBudget`/`MissingPx` — never silently drops a column or exceeds the ceiling). Deliberately has no DirectWrite/Direct2D dependency; real text measurement is a `TextMeasurer` (DirectWrite wrapper) that Phase 3 will inject as measured column-content widths — kept separate so this class stays trivially unit-testable.
- [x] `v3/src/IracingLiveCoach.OverlayHost/Layout/WidgetPlacement.cs` — free per-widget X/Y/monitor/anchor/scale placement (spec §4), `WidgetPlacementStore` with a memento-based undo/redo stack, `FuelRelativeLink` (off by default per spec), and `ClampToVirtualDesktop` for recovering an off-screen widget after a monitor/resolution change.
- [x] `v3/src/IracingLiveCoach.OverlayHost/Layout/EditModeHitTester.cs` — manual mouse hit-testing (Direct2D has no input routing WPF gave for free): `HitTest` finds the topmost unlocked widget and zone (body/4 resize corners) under a point; `ApplyDragDelta`/`ApplyResizeDelta` mutate a placement respecting locks and minimum size.
- [x] `v3/tests/IracingLiveCoach.OverlayHost.Tests/` — new test project (net9.0, referencing only the Layout `.cs` files directly since they have no unsafe/GPU dependency, not the whole unsafe OverlayHost project): `WidgetLayoutEngineGeometryTests.cs` (11 tests — the three mandated presets × 100/125/150% DPI, plus two explicit tests proving the exact "480 DIPs ≠ 480 physical px at 150%" trap from spec §17) and `WidgetPlacementTests.cs`/`EditModeHitTesterTests` (24 tests — anchors, undo/redo, locking, clamping, hit-testing, drag/resize). All 35 pass.

**Acceptance — verified:** the geometry test suite (11 tests) confirms all three presets (1 GTP+5 GT3 = 6 rows, 5 SF23 = 5 rows, Relative = 7 rows) either fit within the physical ceiling after shrinking Flexible columns, or honestly report a nonzero `MissingPx` — never silently exceeding 480×378 physical px (the spec's own worked numbers for a 1920×1080 reference monitor, confirmed to match exactly: `floor(0.25×1920)=480`, `floor(0.35×1080)=378`). A dedicated test demonstrates the DPI trap itself: the same DIP-authored column set fits comfortably at 100% but no longer fits at 150% even after every Flexible column shrinks to its minimum. Solution-wide: `dotnet build`/`dotnet test` on the full `.sln` — 111 tests total (38 V2 Core + 38 V3 Core copy + 35 V3 Layout), all green; V2's `src/`/`tests/` trees confirmed to have zero diff.

**Acceptance — NOT verified, honestly flagged:** the "manual test drags each widget on a negative-coordinate second monitor" from the original acceptance criterion needs an actual interactive edit-mode surface to drag *in* — `EditModeHitTester`'s logic is fully unit-tested (including a synthetic off-screen-monitor clamp scenario), but nothing yet renders draggable widgets on screen for a human to literally drag with a mouse. That surface is Control Center/edit-mode territory (Phase 5, or an earlier ad-hoc test harness if useful before then) — deferred honestly rather than claimed done via unit tests alone.

## Phase 3 — Standings + Relative

**Status: started (2026-09-18) — a first real, working slice of Standings; the rest of the phase remains open. This phase is large enough that "scoped" work is happening incrementally rather than in one batch, unlike Phases 0-2.**

- [x] `v3/src/IracingLiveCoach.OverlayHost/Layout/TextMeasurer.cs` — the real DirectWrite text-measurement wrapper Phase 2 deferred (measures actual rendered text via `IDWriteFactory.CreateTextLayout` + `GetMetrics`, kept separate from `WidgetLayoutEngine` so that stays unit-testable without a live device).
- [x] `DeviceResources.cs` refactored: the old monolithic `RenderFrame` split into `BeginFrame()`/`EndFrame()` (device-lost detection unchanged) plus `Context`/`DWriteFactory` properties so widget code can draw through the same device chain. The Phase 0 proof content survives verbatim as `DrawPhase0Proof()` for future sanity checks, just no longer called from the main loop.
- [x] `v3/src/IracingLiveCoach.OverlayHost/Widgets/StandingsWidget.cs` — connects a real `TelemetryReader`, subscribes to `StandingsUpdated` (no recalculation logic duplicated — consumes `StandingsRow`'s already-resolved `ClassColorHex`/`EstimatedDeltaIRating` as-is), and draws: class color strip, position, driver name, and the iRating+Δ **combined badge in the same row** (spec §6 — green/red delta color from `PaletteTokens`). Shows an honest "Aguardando iRacing..." state (muted `TextDisabled` color) when `HasRecentTelemetry` is false or no rows exist yet — never fabricates a row.
- [x] `Program.cs` rewired to create real `TelemetryReader`-backed `StandingsWidget` and draw it every frame via the new `BeginFrame`/`Draw`/`EndFrame` cycle.
- [x] Smoke-tested for real: built Release, ran the exe with no iRacing session active, screenshotted — confirmed "Aguardando iRacing..." renders correctly in the right color, over a genuinely transparent background, no crash, no fabricated data.

**Update (2026-09-18, second pass) — Relative widget, more Standings columns, and real drag-repositioning added:**

- [x] `v3/src/IracingLiveCoach.OverlayHost/Widgets/RelativeWidget.cs` — connects its own `TelemetryReader`, subscribes to `FullRelativeUpdated` (already produces exactly spec §7's mandated 3-ahead/player/3-behind 7-row preset — no row-count logic duplicated). Draws class strip, position offset (`P` for the player's own row, never a fabricated `+0`), name, iRating (no Δ, per spec §7's explicit exclusion), and relative gap.
- [x] StandingsWidget gained three more columns: gap-to-leader, last lap (via the shared `LapTimeFormatting.Format`, not reimplemented), and lap-delta-vs-player (green/red per spec §6's sign convention, neutral `0.000` on the player's own row).
- [x] Spec §12's simulation preview (`SetSimulatedRows` on both widgets, toggled with a `T` key in this prototype) — fictitious rows clearly labeled "SIMULAÇÃO" in `PaletteTokens.Warning`, letting every column be verified visually without a live iRacing session. **This is how the screenshots below were produced** — never used as a stand-in for real telemetry in the actual code path.
- [x] Real drag-repositioning wired end-to-end: `Program.cs` now holds a `WidgetPlacementStore` with one `WidgetPlacement` per widget, an `E`-key edit mode (forces click-through off, draws a focus-colored outline per spec §4's "exibindo limites e alças apenas durante edição" via `DrawEditModeOutlines`), and real `WM_LBUTTONDOWN`/`WM_MOUSEMOVE`/`WM_LBUTTONUP` handling that calls `EditModeHitTester.HitTest`/`ApplyDragDelta` and writes the result back into the store. Both widgets now draw at their current placement, not a hardcoded offset.
- [x] Fixed a real bug found via screenshot review: every numeric `ToString` call was using the OS's current culture (this machine's pt-BR settings rendered gaps like `1,800` using a comma decimal separator) instead of the invariant, period-decimal format `LapTimeFormatting.Format` already uses elsewhere in the codebase. All numeric formatting in both widgets now explicitly passes `CultureInfo.InvariantCulture`.
- [x] Verified all of the above for real: built Release, launched, pressed `T` then `E`, screenshotted (both widgets rendering every column correctly, edit-mode outline visible), then simulated an actual mouse drag (`mouse_event` LBUTTONDOWN → move → LBUTTONUP) on the Standings widget and screenshotted again — **confirmed the widget visibly moved to the dragged position with the outline following it**. This closes the "manual drag test" gap Phase 2 had left open, at least for this single-window prototype.

**Update (2026-09-18, third pass) — flags and brand text wired in:**

- [x] Initial emoji-only Flag column was replaced after auditing the V2 implementation: V2's `FlagAsset()` uses the real bundled `br.png`, `us.png`, and `jp.png` files, rather than relying solely on emoji. V3 copies those exact source files into its isolated output and `FlagBitmapCache` decodes them once with WIC into device-local `ID2D1Bitmap`s. Standings and Relative draw those bitmaps with high-quality cubic filtering. No country-code or emoji rendering path remains; an unmapped asset is intentionally blank rather than a misleading textual substitute.
- [x] Both widgets now draw a Brand column using `row.ManufacturerBadge` as plain text — the same fallback V2's own `BrandIcons.cs` uses for a manufacturer with no vector mark (e.g. Mercedes). A real bitmap/vector icon pipeline is still open (see below).
- [x] Found and fixed a real bug via screenshot review: text formats had no `WordWrapping.NoWrap` set, so a long brand name ("Aston Martin") wrapped onto a second line and visually overlapped the row below it. All three text formats in both widgets now set `NoWrap` explicitly.
- [x] Verification after the bitmap change: V3 builds, all 111 solution tests pass (38 V2 Core + 38 V3 Core + 35 V3 OverlayHost), and a two-second OverlayHost smoke run stayed responsive after WIC decoded all three PNGs. The flag cache also subscribes to `DeviceResources.DeviceRecovered` and recreates only its GPU bitmaps after a device reset.

**Update (2026-09-18, fourth pass) — data completeness and presentation policy:**

- [x] Real car number is carried from `DriverInfo.CarNumber` through V3 `StandingsRow`/`RelativeRow`; it is rendered separately with `#` and never derived from `CarIdx` or display index.
- [x] Flag and manufacturer assets now share one WIC-to-Direct2D cache. The renderer uses the existing transparent V2 PNGs for both categories, preserves original colours, performs no disk I/O per frame, and recreates only GPU bitmaps after device loss. Text remains only the explicit fallback for an unrecognised manufacturer.
- [x] Barlow Semi Condensed Regular/SemiBold is bundled from the official open-font source and registered privately for the OverlayHost process; Standings and Relative no longer request Segoe UI.
- [x] Separate licence/SR and Overtake cells render in both tables. OT keeps seconds and proportional bar together; gray=available, green=active, amber=cooldown, muted=depleted, dash=unknown. Gap and interval are independent Standings columns.
- [x] `StandingsSelection` is a pure, unit-tested policy: Top N is class-local, own/other class budgets remain distinct, player window is centred where available, and overlapping rules cannot duplicate a driver. Standings renders compact class headers only in multiclass sessions; monoclass draws no redundant class legend.
- [x] Relative has a compact session header sourced from telemetry plus local PC clock; no synthetic session values are used.

**Still open (not started or not finished) — being explicit so nothing here is mistaken for done:**
- [ ] Extend flag coverage only from a vetted existing/local source when the currently present V2 asset set is expanded. The V3 renderer deliberately has no generated, emoji, country-code, or guessed-nationality fallback.
- [ ] Relative header needs the remaining configured player/session items (brake bias, track temperature and rubber state) and both table headers still need their full per-widget control surface from Phase 5.
- [x] Standings and Relative now own independent top-level HWNDs, DirectComposition surfaces, device resources and edit-mode dragging. They no longer share a transparent prototype window or its excess area; closing either one does not terminate the other surface. **Corrected 2026-09-18**: an earlier revision of this section had a stale duplicate bullet claiming this was still a single shared window — that was leftover text from before this was implemented, not a real regression; verified by reading `Program.cs` (six independent `CreateOverlayWindow`/`DeviceResources` pairs, one per widget).
- [ ] A live, in-game verification with real telemetry (this session verified the "disconnected" state and the full simulation preview, but the actual live `StandingsUpdated`/`FullRelativeUpdated` data path has not been visually confirmed against a real iRacing session).

**Acceptance criteria from the original scope (OT balance+bar, multiclass grouping, Top N, asset icons, etc.) remain the target — a meaningfully larger slice is done and verified now, but the phase is not complete.**

## Phase 4 — Weather, Fuel, Radar, Start Helper

**Status: rewritten 2026-09-18 after a critical audit found the first pass unacceptable.** The first pass (committed as `21893e6`/`e48eb26`, no body, no test/screenshot evidence) built all four widgets but: drew a decorative title in Fuel ("FUEL CALCULATOR") and Weather ("WEATHER REPORT") — a direct, repeated violation of spec §5/§15's most emphasized rule; Radar rendered identically for "clear track" and "telemetry disconnected/unknown" (spec §10 explicitly forbids this) and never drew the real per-car `RadarBlip` distance list at all; Start Helper never showed RPM despite spec §11 requiring "RPM atual e faixa-alvo" and always used the "in range" color regardless of value, ignoring the `StartHelperOutOfRange`/`StartHelperCritical` tokens built for exactly this; Weather never showed wind, current-rain-vs-forecast, or track rubber state despite `WeatherStatus` already carrying rubber/precipitation data. All four files were also written in a single-line, single-letter-variable style sharply inconsistent with the rest of the codebase. Full findings and the fix are documented here rather than silently overwritten, per this plan's own traceability practice.

- [x] **Titles removed** from Fuel and Weather — no decorative header text in either widget now (spec §5/§15).
- [x] **Radar rewritten**: explicitly distinguishes three states — no telemetry/no confirmed track length ("RADAR —", muted), genuinely clear track ("CLEAR", secondary text), and active blind-spot/blip data (spec §10's required distinction). Now also draws the real `RadarStatus.Blips` list (per-car signed track distance) as a vertical scale, colored critical when a car is within 10m — previously completely unused despite the data model already existing.
- [x] **Start Helper rewritten**: adds a live RPM readout (added `RaceStartStatus.RpmValue`, sourced from the same real `"RPM"` SDK channel already read elsewhere in `TelemetryReader`) with tri-color state (`StartHelperInRange`/`OutOfRange`/`Critical`) against a **documented default placeholder band** (5500–7000 RPM target, 8500 critical) — honestly not a real per-car calibration, which needs Phase 5's Control Center to be meaningful.
- [x] **Weather rewritten**: adds wind (new `WeatherStatus.WindSpeedMs`/`WindDirectionDeg`, read from the real `WindVel`/`WindDir` SDK channels, independently optional like every other field here) and draws the already-existing `TrackRubberState` and `PrecipitationPct` (explicitly labeled "RAIN NOW", never as a forecast/probability, per spec §8's warning against conflating the two).
- [x] All four rewritten in the established readable style (doc comments, named locals, no line-jamming), matching Standings/Relative/FlagBitmapCache.
- [x] Verified for real: full solution build + `dotnet test` (all 113 tests green), then built Release and ran the actual exe with no iRacing session active — screenshotted and confirmed Weather/Fuel show "Aguardando iRacing...", Radar shows "RADAR —" (the disconnected state, correctly distinct from "CLEAR"), and nothing crashes. Start Helper correctly shows nothing (`ShouldShow` is false outside a stationary race start, which is correct per spec §11, not a bug).

**Files:** `v3/src/IracingLiveCoach.OverlayHost/Widgets/WeatherWidget.cs`, `FuelWidget.cs`, `RadarWidget.cs`, `StartHelperWidget.cs`.

**Still open, honestly:**
- [ ] Start Helper's RPM target band is a generic placeholder, not real per-car calibration (needs Phase 5).
- [ ] Fuel is missing a distinct "last valid lap" consumption figure and a conservative-mode toggle (spec §9) — `FuelStatus` only carries a rolling average; no config surface exists yet to offer a margin/conservative mode.
- [ ] Radar's blip vertical scale is a reasonable interim visualization, not yet validated against spec §10's exact "multiplicidade quando disponível" wording with real multi-car data (no live session tested this turn).
- [ ] A live, in-game verification with real telemetry for all four widgets (this pass only verified the honest "disconnected"/"waiting" states, same limitation as Phase 3).

**Acceptance:** Radar shows symbolic left/right/both indicators and real per-car distances only if telemetry supports it (no fabricated XY positions per spec §10) and never conflates "clear" with "unknown" — **done**. Start Helper shows RPM+target-range+tri-color state using real telemetry against a documented placeholder calibration — **done, calibration itself still a placeholder**.

## Phase 5 — Control Center V3 (Layout tab started, real IPC working)

**Status (2026-09-18):** the bare scaffold noted below was rewritten into a real, working Layout tab with a genuine typed IPC channel to the running overlay — not just UI that looks plausible. **Verified end-to-end**, not just built: launched both `IracingLiveCoach.OverlayHost.exe` and `IracingLiveCoach.ControlCenter.exe` for real, selected Standings, changed X from 200 to 600 in the panel, and screenshotted before/after — the "Aguardando iRacing..." overlay text visibly moved from x≈200 to x≈600 on screen, with the panel showing "Connected — overlay updated live." This is the same rigor as every other phase in this plan: a claim backed by a screenshot, not an assertion.

- [x] `v3/src/IracingLiveCoach.OverlayHost/Ipc/PlacementMessage.cs` + `PlacementIpcServer.cs` — a named pipe (`iracinglivecoach-v3-placement`) server, one fresh `NamedPipeServerStream` per connection so a Control Center that isn't running yet, or restarts, never breaks the loop (this **is** spec §3's "reconexão", achieved by never depending on a long-lived session rather than by explicit retry logic). Messages carry an explicit `SchemaVersion`; an unrecognized version is ignored, never guessed at.
- [x] `v3/src/IracingLiveCoach.ControlCenter/PlacementIpcClient.cs` — opens a short-lived connection per edit (200ms connect timeout) and sends one JSON line; failure (overlay not running) is reported to the user in the panel, never silently swallowed.
- [x] `WidgetPlacement` gained `Visible`/`Opacity` fields (spec §5/§12), added as trailing optional parameters so every existing positional construction — including all 35 unit tests — kept compiling unchanged.
- [x] `Program.cs`'s `ApplyPlacementMessage` applies position/visibility/opacity/lock **live** via `SetWindowPos`/`ShowWindow`/`SetLayeredWindowAttributes`, no restart, matching spec §3's core promise.
- [x] Rewrote the previously-broken stub `MainWindow.xaml.cs` (its `Slider.ValueChanged` handler was typed `RoutedEventArgs`, the wrong type for that event; it also used a different pipe name/message shape than any server ever listened on, so it could never have worked) into a real, correctly-typed Layout tab: numeric X/Y/Width/Height fields, Visible/Lock checkboxes, Opacity/Scale sliders, all sending live on every change. Also fixed a real omission: `App.xaml` had no `StartupUri`, so the previous stub's window would never have appeared on launch at all.

**Still open, honestly:**
- [ ] Width/Height/Scale changes update `PlacementStore` but do not yet resize the live swap chain — `DeviceResources` has no `ResizeBuffers` path yet, so a widget's rendered content stays its original size even though the stored placement changed. Only X/Y/Visible/Opacity/Locked are truly live end-to-end right now.
- [ ] No read-back: the panel does not query the overlay's current placement, so the numbers shown on launch/widget-select are the panel's own hardcoded defaults (mirroring `Program.cs`'s initial values), not a live poll. Selecting a widget the user already repositioned via in-game drag would show stale numbers until the user touches a field.
- [ ] None of spec §12's other tabs exist: Cabeçalhos, Colunas (per-column show/hide/width/decimals — the actual point of a "Control Center" for Standings/Relative), Aparência, Cores, Regras, Perfis. This is Layout only.
- [ ] Snap/grid, per-widget lock enforcement in-panel (the checkbox sends `Locked` but the panel itself doesn't prevent editing a locked widget's fields), z-order controls, and the Fuel↔Relative link toggle are all unbuilt.
- [ ] Monitor selection/anchor point (spec §4) aren't exposed in the panel yet — X/Y are always relative to the primary monitor's origin.

**Files:** `v3/src/IracingLiveCoach.OverlayHost/Ipc/PlacementMessage.cs`, `PlacementIpcServer.cs`; `v3/src/IracingLiveCoach.ControlCenter/PlacementIpcClient.cs`, `MainWindow.xaml(.cs)`, `App.xaml`.

## Phase 6 — Profiles & Persistence (versioned save/load working, wired into startup + live edits)

**Status (2026-09-18):** built `PlacementPersistence.cs`, **not** as a copy of V2's `WidgetLayoutStore.cs` as this plan originally said. On inspection, V2's `WidgetLayoutStore`/`WidgetLayout` shape doesn't match V3's `WidgetPlacement` model at all, V2's own active UI doesn't even use `WidgetLayoutStore` (it persists through `ProfileStore.cs`/`v2-profile.json` instead), and putting V3's placement-persistence logic in `IracingLiveCoach.Core` would create a wrong-direction dependency (`WidgetPlacement` lives in `OverlayHost.Layout`, not Core). Deviation and rationale are documented directly in `PlacementPersistence.cs`'s own doc comment, same practice as every other plan-vs-codebase mismatch found so far.

- [x] `v3/src/IracingLiveCoach.OverlayHost/Persistence/PlacementPersistence.cs` — `PlacementProfile` record (`SchemaVersion`, `Widgets`, `FuelRelativeLinkEnabled/SpacingDip`) with `CurrentSchemaVersion = 1`. `Load`/`Save`/`TryExport`/`TryImport`, all going through a private `ApplyProfile` that rejects (never partially applies) a profile whose `SchemaVersion` doesn't match — spec §3's "migrações" seam, one version deep so far.
- [x] **Atomic writes** (spec §3 "gravação atômica"): `Save` writes to `<path>.tmp` then `File.Move(..., overwrite: true)` in the same directory — genuinely atomic, unlike V2's own `WidgetLayoutStore.Save()` which does a direct `WriteAllText`.
- [x] Wired into `Program.cs`: `PlacementPersistence.Load(PlacementStore)` runs right after the six hardcoded defaults are set and before any window/DeviceResources creation, so a saved layout overrides the defaults but a first-ever launch (no file yet) still starts sane. `PlacementPersistence.Save(PlacementStore)` runs inside `ApplyPlacementMessage`, right after every Control-Center-driven edit — every applied change survives the next launch.
- [x] Wired into the in-game edit-mode drag handler too: `WM_LBUTTONUP` previously only called `SetWindowPos` and forgot the widget's placement entirely, so a drag performed directly on the overlay (as opposed to through the Control Center) silently reverted on the next launch and left the panel showing stale numbers. Added a `Dictionary<nint, string> WidgetKeysByHandle` reverse lookup, and on drag-end (`_editMode && _draggingWindow == hwnd`) it now reads the window's final position via `GetWindowRect`, updates `PlacementStore`, and calls `PlacementPersistence.Save` — the same path `ApplyPlacementMessage` uses.
- [x] **Verified live, not just built:** launched `IracingLiveCoach.OverlayHost.exe`, sent a real IPC message over the named pipe moving Standings to X=333/Y=444, confirmed `%APPDATA%\iracing-live-coach\v3-layout.json` was written with those exact values (atomic temp-file-then-rename, file readable immediately after). Killed the process, relaunched it, and enumerated its windows via `EnumWindows`/`GetWindowRect`: the Standings window came back at `Left=333 Top=444` — the saved layout genuinely overrode the hardcoded defaults on a cold restart.
- [ ] The `WM_LBUTTONUP` drag-end persistence fix above is code-consistent with the already-verified IPC save path (identical `PlacementStore.Set` + `PlacementPersistence.Save` calls) but was **not independently re-verified live** — that would need driving a real mouse drag through edit mode (`E` to toggle, click-drag, release) rather than just an IPC message, which wasn't done this pass.
- [ ] No Control Center UI for import/export, undo/redo, or profile duplication/renaming (spec §12) — only the static-file `TryExport`/`TryImport` functions exist; nothing in `MainWindow.xaml` calls them yet.
- [ ] Migration logic is a documented seam, not proven machinery — only `SchemaVersion = 1` exists, so there's no real migration path exercised yet, only the branch where one would go.

**Files:** `v3/src/IracingLiveCoach.OverlayHost/Persistence/PlacementPersistence.cs` (new); `v3/src/IracingLiveCoach.OverlayHost/Program.cs` (Load/Save wiring, `WidgetKeysByHandle`, `WM_LBUTTONUP` drag-end sync).

## Phase 7 — Validation, Benchmarking, Packaging (scoped)

**Files:** `scripts/publish-v3.ps1` (parallel to existing `scripts/publish.ps1`, publishing to `%LOCALAPPDATA%\IracingLiveCoachV3\` so V2 and V3 can run side-by-side without collision). Benchmark harness logging frame latency/CPU/GPU/memory per spec §13, captured on the reference hardware.

---

## Traceability Matrix — Full Spec Coverage

Every section of the spec (nothing removed, nothing paraphrased away) mapped to where it is executed. This exists so a gap is visible immediately instead of surfacing mid-implementation. If a future edit to this plan would leave a spec line with no phase, that edit is wrong — add coverage, don't drop the line.

| Spec § | Requirement | Phase(s) |
|---|---|---|
| §1 | Inspect current repo/branch/commit, preserve uncommitted work, inventory all 6 widgets' features/calculations/telemetry/config/fixes, classify reuse vs. rebuild, verify P2P fix + SDK indexing semantics before assuming a bug, document real per-car/session capabilities (own/opponent/missing/stale/derived) | Phase 1 |
| §2 | Closed scope: exactly Standings, Relative, Weather, Fuel, Radar, Start Helper — no map/inputs/session-summary/delta widgets; one shared visual system; class grouping is the only structural difference; Overtake is conditional by capability | Global Constraints; Phase 3/4 |
| §3 | Core/adapter/OverlayHost/ControlCenter split; typed versioned local IPC with reconnect; shared declarative layout engine (not per-table hardcoded positions); preview and real overlay share the engine; versioned persistence w/ migrations, atomic writes, import/export, restore defaults; GPU technical proof BEFORE full build-out; document real limitations (no bare "as fast as Kapps" claims); separate acquisition/calc/present; render latest valid state, no growing queues, no heavy work on render thread; cache fonts/icons/geometry/static text; re-layout only on structural/config change; handle device-lost/disconnect/session-change/monitor-removed | Phase 0 (proof), Phase 1 (Core split, IPC, device-lost), Phase 2 (layout engine, caching) |
| §4 | Fully free per-widget positioning on any monitor; drag in edit mode + panel preview; X/Y/monitor/anchor/scale/dimension editing; negative virtual-desktop coordinates, DPI-safe, resolution/monitor-change recovery; optional alignment guides/grid/snap (never mandatory); per-widget/global lock; configurable z-order; race mode click-through without stealing focus; edit-mode shortcut showing bounds/handles only while editing; Fuel starts left of Relative, independent, optional off-by-default link with configurable spacing; undo/redo of layout changes, off-screen widget recovery; save position/size/visibility/appearance per profile; closing the panel doesn't close overlays; no font compression to fit width; resize respects explicit fixed/flexible column policy and minimums, no distortion or silent data loss | Phase 2 (`WidgetPlacement`/`EditModeHitTester`), Phase 5 (Layout tab), Phase 6 (per-profile save) |
| §5 | Broadcast-style graphite translucent surfaces, high contrast, compact rows, subtle borders, minimal rounding; no big cards/heavy shadows/decorative titles/excess whitespace; normative palette (§16) centralized as tokens, not scattered HEX; Barlow Semi Condensed primary, Rajdhani optional for numerals only if legible; licensed font resources + fallback; no scaleX/scaleY letter distortion; tabular numerals, decimal alignment; 1080p/100% starting metrics (12–13px text, 22–24px row, 2–4px cell padding, 14–18px flags/badges) as configurable, verified-not-assumed starting points; full first+last name by default, explicit opt-in abbreviation/ellipsis only, no automatic per-name font shrink; correct-proportion brand/flag assets from local quality sources, no nationality guessing from name/club; no decorative widget titles or column-header rows during a race — only §15's session-info headers; Control Center itself still names widgets normally; track center stays clear in the initial composition; independent background/content opacity, per-widget scale, row-height adjustment | Global Constraints; Phase 2 (`PaletteTokens`, layout engine); Phase 3/4 (naming/asset rules) |
| §6 | Standings columns (class/overall position, class color strip, car number, flag, brand, name, licence/SR, iRating, ΔiRating, gap, interval, last lap, last-lap-vs-player delta), pit/DC/lapped states when supported; iRating+Δ same rounded badge same row, neutral bg/light iRating/green-pos/red-neg, licence badge separate, configurable badge width, no duplicate delta elsewhere; Δ is an estimate — document formula/eligibility/limits, no estimate without valid support; last lap m:ss.sss configurable precision; lap-delta = opponent last valid lap − player last valid lap (sign convention as specified), player shows 0.000, no valid lap → "—", never confused with race gap; gap/interval have defined configurable reference, correct multiclass/lapped handling; multiclass compact class headers, configurable class order, player-class highlight, independent limits per own-class vs. others, per-class override; SF23 single table, same components, Overtake as optional column when available; configurable fixed Top N per class combined with player-window, no duplicates, Top N counts within that class's row budget, validate incompatible configs; Top N=1 + 5 own-class example behavior; discreet separator for omitted positions, headers/separators don't count as drivers; multiclass preset = 1 GTP + 5 GT3 (player assumed GT3); SF23 preset = 5 drivers; don't hardcode manufacturer/car counts — derive from grid/validated catalog | Phase 3 |
| §7 | Relative = temporal track proximity, not a second classification; documented handling of lapped cars, pit lane, stopped cars, line-crossing; preset 7 drivers (3 above/player/3 below), configurable count/centering, no fictitious filler drivers; columns: position, class color strip, car number, flag, brand, first+last name, licence/SR, iRating, relative gap — no ΔiRating here; full-width configurable top header (player's current brake bias, track temp, rubber when available, PC local clock), no empty footer or duplication at table bottom, coexists with Weather Report, missing fields show "—" or hide per config, no inventing rubber level from session time; Overtake: seconds not percentage, 200s preset reference validated not extrapolated to other cars; balance and state (available/triggered/blocked-cooldown/depleted/unsupported/unknown) modeled separately; visual = balance in seconds + thin proportional bar only, colors silver/green/amber per §16, no PRONTO/ATIVO/RECARGA words/icons/cooldown countdown in the live cell (explanation lives in the config panel); bar length from validated balance/limit, never cooldown-as-balance, positive balance ≠ available; depleted shows `0 s` + empty track, unknown/unsupported shows "—" with no fake fill; OT column shares exact row height/separators/grid with all other columns, no per-cell colored outline or standalone cards; treat player-field vs opponent-field availability separately, missing data never silently becomes zero/ready/estimated; preserve the current validated P2P fix, don't assume it's broken from old code; SF23 and multiclass share the same Relative widget, OT is a conditional column not a separate widget | Phase 3 |
| §8 | Compact Weather Report: mandatory track wetness, air/track temp, wind+direction, only genuinely available rain/humidity fields; separate current rain from probability/forecast — never label measured intensity as probability, never fabricate forecast; map official wetness values/categories, don't convert to unfounded percentages; configurable units; rubber is a field independent of humidity/wetness; local clock in top headers — per-widget opt-in, enabled by default in Relative, available in Standings incl. class-name band; source is OS local clock (PC timezone), never virtual session time; HH:mm / HH:mm:ss formats, optional seconds, "LOCAL" short label, update only as often as needed | Phase 4 |
| §9 | Fuel: current fuel, last-valid-lap consumption, average, conservative value, range, laps/time remaining, fuel-to-finish, margin, amount-to-add when calculable; fuel/range emphasized, other metrics smaller; configurable units, decimals, averaging window, conservative mode, margin (liters/laps), field visibility; defined handling for pit exit, refueling, incomplete lap, session reset, insufficient sample; timed races need explicit remaining-lap estimation, no false precision; any refuel automation is an explicit, supported, validated opt-in, never a calculation side-effect — minimum scope is calculate-and-display | Phase 4 |
| §10 | Audit real available telemetry before choosing representation; if only lateral-proximity states exist, use symbolic left/right/both + multiplicity indicator, no precise XY car positions or meter distances from insufficient data; free position/size; configurable color/intensity/orientation/hide-when-clear; responsive priority updates; optional visual smoothing must not delay occupancy alerts or invent trajectory; distinguish clear track from disconnected/unknown telemetry | Phase 4 |
| §11 | Compact Start Helper: current RPM + target range, current clutch + configured/calibrated reference, readiness states and start conditions when available; no fabricated official lights or ideal-moment-from-arbitrary-timer; per-car profiles, calibration, tolerances, colors, auto-visibility configurable; shown during prep, hidden post-start per configurable rule/delay, forced preview in panel; same fast architecture as Radar, not folded into another widget | Phase 4 |
| §12 | Full graphite control panel consistent with overlays; sidebar with 6 widget toggles; preview; tabs Layout/Cabeçalhos/Colunas/Aparência/Cores/Regras/Perfis; changes apply live to preview+widget with no restart; per-column: show/hide, drag/keyboard reorder, restore default, individual/min width, fixed/flexible mode, alignment, padding, per-field decimals, time/unit format, explicit delta sign, header config, conditional colors, missing-data representation; table auto-width = visible columns + paddings + separators + borders; changing one column resizes predictably, never stretches font/other columns; session headers adapt to resulting width within §15/§17 limits; no silent field-hiding; no purely decorative controls; per-widget: position/monitor/anchor/scale/visibility/opacity/font/row-height/borders/session-header/per-session rules; Standings gets Top N/rows-per-class/player-window; Relative gets above/below counts/class filters/OT; global/per-car/class-series/session profiles with explicit precedence, manual override, duplicate/rename/import/export/undo/restore/save-as; no unexpected layout change mid-race; fictional-data preview clearly labeled as simulation, available without iRacing open | Phase 5, Phase 6 |
| §13 | Reference hardware (5500X3D/GTX1660/16GB/1080p165Hz, borderless); other modes announced only after testing; configurable pacing incl. 60/120Hz/monitor rate; render rate ≠ SDK data rate, no invented sample rates or promised 165 reads/sec; measure snapshot-to-present latency, frame time p50/p95/p99, CPU/GPU/memory/GC/dropped updates/game-FPS impact; compare game with/without overlay incl. 6 widgets, incl. rain/full grid; define budget after the technical proof, report real results, no Kapps-equivalence claim without comparable measurement; test 100/125/150% DPI, resolution change, secondary monitor, long names, minimum columns, missing data, reconnection, class/session change, refueling, OT active/cooldown/depleted, high CarIdx; tests cover real rules/risks; technical logs stay in diagnostics, never pollute the race UI | Global Constraints; Phase 0 (pacing spike); Phase 7 (full benchmark) |
| §14 | 7-step sequence: (1) inventory+capability map incl. last P2P fix, (2) GPU proof with sharp text/transparency/free positioning/responsive Radar-StartHelper, (3) shared layout/config/preview engine + faithful visual system, (4) Standings+Relative fully editable/grouped/Top-N/valid data, (5) Weather/Fuel/Radar/StartHelper complete, (6) Control Center/profiles/import-export/layout recovery, (7) visual+performance validation, executable package, usage instructions | Phase 0→7 (1:1 mapping, see Scope Decomposition table) |
| §14 (acceptance) | 6 independently-positionable widgets; no decorative titles; full names; valid brand/flag origin; ΔiRating only in Standings; correct lap-delta reference; OT in seconds + colored bar with internally-separate state; track wetness; local clock; 7 Relative rows; 1 GTP+5 GT3 preset; 5 SF23 preset; Top N within limit; Fuel left-of-Relative with link off by default; all widths/orders/precisions persisted; preview/real-render coherence; missing data never masked; deliver real captures of panel+overlay in both scenarios incl. position/column editing; distinguish mockup vs. simulated preview vs. in-race capture; list remaining limitations with evidence, no untested-environment claims | Phase 3–7 acceptance criteria; Phase 0/7 findings docs |
| §15 | No decorative titles, no column-name row, in Standings/Relative; multiclass class-name band + selected items same line; monoclass same structure (e.g., SF23+lap+SoF+clock); independent activated-item list + custom order per widget; Standings allows common-to-classes selection + per-class override, distinguishing session/class/player fields; catalog: session type/state, lap current/total, time remaining, flag, class/overall SoF, class/overall driver count, player incidents/limit, brake bias, air/track temp, wetness, rubber, PC local clock — no fields invented without a reliable source; measure actual rendered text, distribute free space with consistent padding/separators across full width, no arbitrary font growth or letter-stretching; recompute distribution on width/order/precision/unit/content change; reserve space for expected formats to avoid jumps on numeric updates; short labels + item selection; if minimum sum doesn't fit, explain in panel and keep last valid config — never exceed widget limit, truncate values, or hide items without explicit choice; no mandatory footer in Standings/Relative — former footer info moves to the top | Phase 2 (layout/header engine), Phase 3 (Standings/Relative headers), Phase 5 (header config UI) |
| §15 (position/class/number) | Thin vertical strip (3 physical px @1080p) beside position, on ALL Standings/Relative rows; key is session CLASS id only — never manufacturer/car/team/position/licence/country/row order; GTP all-red = GTP header color; GT3 all-yellow = GT3 header color; SF23 all-red in monoclass preset (sharing red across separate presets is intentional; distinct classes in one session stay visually distinct); single `ClassId → ClassColor` map feeds header+strip+panel legend atomically; player cyan highlight never recolors/covers the class strip; licence has its own scheme; brand badge keeps original art; real race position (not row index); car number is a separate field, `#`-prefixed by default, preserving source leading zeros; CarIdx ≠ car number; initial column order strip/position, number, flag, brand, first+last name, licence/SR, iRating, metrics — reorderable, strip stays bound to the position cell | Phase 3 (`PaletteTokens` ClassId map, Standings/Relative rendering) |
| §16 | Full normative HEX palette (surfaces/text/controls table + classes/licences/semantic-state table) with alpha rules (alpha only on the indicated surface, no inherited text-opacity reduction, sRGB, multicolor brand/flag assets keep original colors, no global tint); no zebra striping by default, no default shadow/glow; initial rounding (4px widget/3px badge, no exaggerated pill shapes); 1px physical borders/grid; theme is a customizable/restorable starting point per token, class palette, and profile — not a claim of official series colors; OT legends live in the panel, never in cells; gap color must not imply ahead/behind is inherently good/bad — neutral sign+text in the preset | Phase 2 (`PaletteTokens`, every value from the table) |
| §17 | 1920×1080: no widget over 480 physical px width (25%); 7-driver widget over 378 physical px height (35%) incl. headers/separators/borders/any footer; same percentage ceiling scales to other resolutions/target areas; never use combined multi-monitor width to inflate a single-monitor widget; distinguish viewport vs. monitor dimension in config; these are ceilings not targets (7×24px rows + 24px header ≈194px, don't pad to 378px); validate post DIP→physical-pixel conversion and post widget-scale — 480 DIP ≠ 480 physical px at 150% DPI; formula = `floor(0.25 × target physical width)` / `floor(0.35 × target physical height)` for 7 drivers; column/font/DPI/profile/row-count/data/header changes must never silently exceed the limit — measure real text at final composed dimension; individual column widths stay free within budget, only explicitly-flexible columns adjust to their minimums, reject over-budget configs in the panel with exact missing-px count and offer padding/global-font reduction (within legible minimum) or explicit field-disable, keep last valid config during a race; never solve overflow via typography distortion, per-name shrinking, silently dropping required info, or rasterizing/shrinking the whole table like a photo; first+last name by default, explicit-opt-in ellipsis for exceptionally long names, no blanket initials without authorization; complete presets fit within 480px at legible 12px body/11px metadata/22–24px row height, validated at 100% on a real monitor not just zoomed screenshots — report conflicts with measurements if fields don't fit legibly; panel shows final physical width/height, percentages, limit, and available column width; preview includes real-scale 1920×1080 mode + frame-without-altering-layout option; geometry checks for all presets (1GTP+5GT3, 5SF23, Relative-7) plus font/DPI/long-number/long-name cases; no widget covers track center in the initial preset | Global Constraints; Phase 2 (geometry test), Phase 5 (panel dimension readout) |
| §18 | Reuse existing Live Coach assets/mapping FIRST — don't assume files/formats are pre-audited, inspect the real repo and record real paths/resolution/transparency/origin/problems; inventory manufacturer badges/flags/icons/car catalog, reuse good files and existing resolution logic, no incompatible parallel catalog; resolve `CarId → manufacturer → asset` via stable validated catalog/SDK identifiers, brand never determines class color, same manufacturer across GT3/GTP keeps correct badge but gets its own class's strip color; prefer existing faithful vector source (SVG/compatible vector geometry), normalize viewBox/bounds/padding without redesigning the mark, rasterize vectors to GPU resources at required physical size and cache; for existing bitmaps, preserve PNG transparency/quality, use higher-res variants when available with proper downscale filtering, never upscale pixelated thumbnails or use JPEG-with-background, never auto-vectorize a bad badge by inventing detail; initial badge box 16×16 physical px configurable, proportional `contain` fit, vertically centered, no distortion/cropping, flag in proportional box e.g. 18×12, optical per-brand padding normalization allowed; preserve official asset colors/knockouts/outlines/transparency, no class-color or player-cyan tint on badges, use existing light/dark variant for contrast without altering brand identity; no emoji/letter substitutes/AI-drawn/geometric-approximation as a final badge — neutral local placeholder + logged absence if a resource is missing, never mismatched brand to fill space; flag depends on reliable nationality/country code, missing country uses placeholder, never inferred from name/team/club without validated mapping; load/decode outside the render loop, cache by asset+physical size+DPI+variant, recreate GPU-dependent resources after device-lost, no in-race downloads; same resources/measurements in preview and overlay, validate against grid's actually-present manufacturers, no fixed per-class car count | Phase 3/4 (asset reuse from `BrandIcons`/`BrandImageLoader`/`CountryFlags`); Phase 1 (device-lost recreation hook) |
| §19 | Antialiasing is an acceptance requirement, not just a checked option; DirectWrite grayscale AA on transparent overlay surfaces, no forced ClearType-over-transparency (unpredictable results per MS docs), opaque panel surface may use the appropriate renderer mode; vector geometry/curves/rounded corners/icons via `D2D1_ANTIALIAS_MODE_PER_PRIMITIVE`, never globally aliased; consistent premultiplied-alpha composition path for surfaces/bitmaps that require it, no double-multiplying alpha, Direct2D brush inputs stay straight-alpha per API, avoid black/white halos at transparent edges; per-monitor DPI awareness, recreate targets/caches at correct physical size on DPI/monitor change, never let Windows stretch a low-res image; align grid line boundaries to physical pixels to avoid blur/doubling, no universal blanket 0.5-DIP offset without considering thickness/transform; text measured and drawn at final scale with the same font/weight/params, no re-transforming text bitmaps per resize, badges never clip glyphs/signs/accents/descenders; bitmaps use linear/high-quality reduction filtering, never nearest-neighbor for logos/flags/curves, regenerate a proper variant if downscaling degrades a resource; no global FXAA/TAA/blur to mask UI jaggies, no whole-desktop supersampling — per-resource quality choice with measured cost, text/geometry/image-sampling AA are distinct responsibilities; equivalent colors/alpha/composition order between preview and screen, legible on light/dark/sky/asphalt/rain backgrounds without color fringing; verify uncompressed PNG captures at 100/125/150% DPI and supported widget scales, inspect at real size and magnified for letters/diagonals/curved badge edges/flags/badge borders/OT bars, repeat during motion to catch flicker; deliver evidence of AA configured in code + real captures, no absolute no-jaggies claim from mockups alone, fix perceptible artifacts at homologated scales before acceptance | Global Constraints (AA requirement); Phase 0 (proof: grayscale AA, DPI recreation); Phase 2/3 (per-primitive AA, alpha handling, pixel-aligned grid, text caching); Phase 7 (capture/inspection evidence) |
| §20 | Full consolidated checklist (14 items) — every item is a restatement of requirements already listed above under its originating section | Cross-cutting: see the § rows above for each checklist item's originating requirement and owning phase |

If you ever ask "did the plan keep everything from the prompt," this table is the answer key — every row traces to a spec section and a phase, and none were dropped to shorten the plan.

---

## Execution Handoff

Phase 0 is the next actionable work and is fully detailed above. Two options:

1. **Subagent-Driven (recommended)** — dispatch a fresh implementer subagent for Phase 0's task, review the diff, then decide with you whether to write Phase 1's detailed plan.
2. **Inline Execution** — implement Phase 0 directly in this session with checkpoints.

Both stop at the Phase 0 findings doc for your sign-off before Phase 1 gets its own detailed plan — per the spec's own sequencing (§14, step 2 gates step 3).
