<Window x:Class="IracingLiveCoach.V2.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks" xmlns:svg="clr-namespace:SharpVectors.Converters;assembly=SharpVectors.Converters.Wpf" Title="Live Coach V2 · Overlay" WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ShowInTaskbar="True" WindowState="Maximized">
  <Window.Resources>
    <LinearGradientBrush x:Key="HotLine" StartPoint="0,0" EndPoint="1,0">
      <GradientStop Color="#FF4DE95F" Offset="0" />
      <GradientStop Color="#FF32DDB3" Offset=".58" />
      <GradientStop Color="#FF33DAEC" Offset="1" />
    </LinearGradientBrush>
    <Style x:Key="Label" TargetType="TextBlock" BasedOn="{StaticResource WidgetLabel}">
      <Setter Property="Foreground" Value="#FFF4F7FA" />
      <Setter Property="FontSize" Value="8.5" />
      <Setter Property="FontWeight" Value="Bold" />
      <Setter Property="HorizontalAlignment" Value="Center" />
      <Setter Property="TextAlignment" Value="Center" />
    </Style>
    <Style x:Key="Tiny" TargetType="TextBlock" BasedOn="{StaticResource WidgetValue}">
      <Setter Property="FontSize" Value="10" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
  </Window.Resources>
  <Grid UseLayoutRounding="True" TextOptions.TextFormattingMode="Ideal" TextOptions.TextRenderingMode="Grayscale" TextOptions.TextHintingMode="Animated" SnapsToDevicePixels="True">
    <ItemsControl ItemsSource="{Binding Widgets}">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <Canvas />
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
      <ItemsControl.ItemContainerStyle>
        <Style TargetType="ContentPresenter">
          <Setter Property="Canvas.Left" Value="{Binding Left}" />
          <Setter Property="Canvas.Top" Value="{Binding Top}" />
          <Setter Property="Visibility" Value="{Binding WidgetVisible, Converter={StaticResource BoolToVisibility}}" />
        </Style>
      </ItemsControl.ItemContainerStyle>
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Width="{Binding Width}" Height="{Binding Height}" BorderBrush="#35FFFFFF" BorderThickness="1" CornerRadius="12" Padding="4" Opacity="{Binding Opacity}" Cursor="SizeAll" PreviewMouseLeftButtonDown="OnWidgetMouseDown" PreviewMouseMove="OnWidgetMouseMove" PreviewMouseLeftButtonUp="OnWidgetMouseUp">
            <Border.Style><Style TargetType="Border"><Setter Property="Background" Value="#BC03070B"/><Style.Triggers><DataTrigger Binding="{Binding IsSingleClassSession}" Value="True"><Setter Property="Background"><Setter.Value><LinearGradientBrush StartPoint="0,0" EndPoint="1,1"><GradientStop Color="#D3081E4D" Offset="0"/><GradientStop Color="#C1041027" Offset="1"/></LinearGradientBrush></Setter.Value></Setter></DataTrigger></Style.Triggers></Style></Border.Style>
            <Grid ClipToBounds="True">
              <Grid Margin="0" RenderTransformOrigin="0,0">
                <Grid.LayoutTransform>
                  <ScaleTransform ScaleX="{Binding FontScale}" ScaleY="{Binding FontScale}" />
                </Grid.LayoutTransform>
                <Grid.RowDefinitions>
                  <RowDefinition Height="{Binding HeaderHeight}" />
                  <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Visibility="{Binding ShowWidgetHeader, Converter={StaticResource BoolToVisibility}}">
                  <ItemsControl ItemsSource="{Binding HeaderFields}" HorizontalContentAlignment="Stretch" Visibility="{Binding IsTimingWidget, Converter={StaticResource BoolToVisibility}}"><ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Rows="1"/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemContainerStyle><Style TargetType="ContentPresenter"><Setter Property="Visibility" Value="{Binding IsVisible, Converter={StaticResource BoolToVisibility}}"/></Style></ItemsControl.ItemContainerStyle><ItemsControl.ItemTemplate><DataTemplate><Grid HorizontalAlignment="Center" VerticalAlignment="Center"><StackPanel Orientation="Horizontal" HorizontalAlignment="Center"><iconPacks:PackIconMaterial Kind="{Binding Icon}" Width="11" Height="11" Foreground="#FFF4F7FA" Margin="0,0,4,0"/><TextBlock Text="{Binding Value}" Foreground="#FFF4F7FA" FontSize="9" FontWeight="Medium" VerticalAlignment="Center"/></StackPanel></Grid></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                </Grid>
                <Grid Grid.Row="1">
                  <StackPanel Visibility="{Binding IsStandings, Converter={StaticResource BoolToVisibility}}">
                    <ItemsControl ItemsSource="{Binding DataContext.PreviewDrivers, RelativeSource={RelativeSource AncestorType=Window}}">
                      <ItemsControl.ItemTemplate>
                        <DataTemplate>
                          <Border Height="30" BorderBrush="#263E464D" BorderThickness="0,0,0,1" Padding="0,0,2,0">
                            <Border.Style>
                              <Style TargetType="Border">
                                <Setter Property="Background" Value="#10000000" />
                                <Style.Triggers>
                                  <DataTrigger Binding="{Binding IsClassHeader}" Value="True"><Setter Property="Height" Value="37" /></DataTrigger>
                                  <DataTrigger Binding="{Binding IsPlayer}" Value="True">
                                    <Setter Property="Background" Value="#274DE95F" />
                                    <Setter Property="BorderBrush" Value="#FF4DE95F" />
                                  </DataTrigger>
                                </Style.Triggers>
                              </Style>
                            </Border.Style>
                            <Grid>
                            <Grid Visibility="{Binding IsDriverRow, Converter={StaticResource BoolToVisibility}}">
                              <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="{Binding DataContext.PositionColumnWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
                                <ColumnDefinition Width="{Binding DataContext.CarNumberColumnWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
                                <ColumnDefinition Width="{Binding DataContext.DriverColumnWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
                                <ColumnDefinition Width="{Binding DataContext.LicenseColumnWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
                                <ColumnDefinition Width="{Binding DataContext.IRatingColumnWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
                                <ColumnDefinition Width="Auto" />
                              </Grid.ColumnDefinitions>
                                <Border Background="{Binding PositionBackground}" CornerRadius="3" Margin="1,4">
                                <TextBlock Text="{Binding Position}" Style="{StaticResource WidgetNumber}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                              </Border>
                              <TextBlock Grid.Column="1" Text="{Binding CarNumber}" Style="{StaticResource WidgetNumber}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                              <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Width="22" Height="22" Background="Transparent" Margin="0,0,4,0" Visibility="{Binding DataContext.ShowLogos, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource BoolToVisibility}}">
                                  <Image Source="{Binding BrandImage}" Stretch="Uniform" Margin="1" SnapsToDevicePixels="True" RenderOptions.BitmapScalingMode="HighQuality" />
                                </Border>
                                <Border Width="20" Height="14" Margin="0,0,5,0" BorderBrush="#405A6670" BorderThickness="0.5" Visibility="{Binding DataContext.ShowFlags, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource BoolToVisibility}}">
                                  <Grid>
                                    <TextBlock Text="{Binding Flag}" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    <Image Source="{Binding FlagImage}" Stretch="Fill" SnapsToDevicePixels="True" RenderOptions.BitmapScalingMode="HighQuality" />
                                  </Grid>
                                </Border>
                                <TextBlock Text="{Binding Driver}" Style="{StaticResource WidgetValue}" FontSize="11" VerticalAlignment="Center" />
                              </StackPanel>
                              <Border Grid.Column="3" Background="#4A101A22" BorderBrush="{Binding LicenseBrush}" BorderThickness="1" CornerRadius="4" Padding="4,2" Margin="1,4">
                                <TextBlock Text="{Binding License}" FontSize="9" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{Binding LicenseBrush}" />
                              </Border>
                              <Border Grid.Column="4" Background="#FF222B33" BorderBrush="#FF77838E" BorderThickness="1" CornerRadius="4" Padding="4,0" Margin="1,3">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                  <TextBlock Text="{Binding IRatingValue}" FontSize="10.5" FontWeight="Bold" Foreground="#FFF4F7FA" VerticalAlignment="Center" />
                                  <iconPacks:PackIconMaterial Kind="{Binding IRatingArrow}" Width="10" Height="10" Foreground="{Binding IRatingDeltaBrush}" Margin="3,0,1,0" VerticalAlignment="Center" />
                                  <TextBlock Text="{Binding IRatingDelta}" FontSize="9" FontWeight="Bold" Foreground="{Binding IRatingDeltaBrush}" VerticalAlignment="Center" />
                                </StackPanel>
                              </Border>
                              <ItemsControl Grid.Column="5" ItemsSource="{Binding Fields}">
                                <ItemsControl.ItemsPanel>
                                  <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                  </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                  <DataTemplate>
                                    <Border Width="{Binding Width}" BorderBrush="{Binding Accent}" BorderThickness="0,0,0,1" Background="#12000000" Padding="2,0">
                                      <Grid VerticalAlignment="Center"><Grid.RowDefinitions><RowDefinition Height="15"/><RowDefinition Height="9"/></Grid.RowDefinitions><TextBlock Text="{Binding NonP2PValue}" Foreground="{Binding Accent}" FontWeight="Medium" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.RowSpan="2"/><iconPacks:PackIconMaterial Kind="{Binding P2PIcon}" Width="9" Height="9" Foreground="{Binding Accent}" Margin="3,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" Visibility="{Binding IsP2P, Converter={StaticResource BoolToVisibility}}"/><TextBlock Text="{Binding P2PSeconds}" Foreground="{Binding Accent}" FontWeight="Bold" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/><ProgressBar Grid.Row="1" Value="{Binding P2PLevel}" Maximum="100" Foreground="{Binding Accent}" Background="#35414A54" BorderThickness="0" Height="6" VerticalAlignment="Center" Visibility="{Binding IsP2P, Converter={StaticResource BoolToVisibility}}"/></Grid>
                                    </Border>
                                  </DataTemplate>
                                </ItemsControl.ItemTemplate>
                              </ItemsControl>
                            </Grid>
                            <Grid Height="34" Visibility="{Binding IsClassHeader, Converter={StaticResource BoolToVisibility}}">
                              <Border Background="#D6080B0F" CornerRadius="5"/>
                              <Grid Margin="0,2,0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="{Binding ClassHeaderPositionWidth}"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <Border Background="{Binding PositionBackground}" CornerRadius="4" Padding="3,0"><TextBlock Text="{Binding ClassHeaderText}" Style="{StaticResource WidgetTitle}" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Center"/></Border>
                                <ItemsControl Grid.Column="1" ItemsSource="{Binding ClassHeaderFields}" VerticalAlignment="Center"><ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemContainerStyle><Style TargetType="ContentPresenter"><Setter Property="Visibility" Value="{Binding IsVisible, Converter={StaticResource BoolToVisibility}}"/><Setter Property="Margin" Value="9,0,0,0"/></Style></ItemsControl.ItemContainerStyle><ItemsControl.ItemTemplate><DataTemplate><StackPanel Orientation="Horizontal"><iconPacks:PackIconMaterial Kind="{Binding Icon}" Width="10" Height="10" Foreground="#FFF4F7FA" Margin="0,0,3,0"/><TextBlock Text="{Binding Value}" Foreground="#FFF4F7FA" FontSize="9" FontWeight="Medium" VerticalAlignment="Center"/></StackPanel></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                              </Grid>
                              <Border Height="2" Background="{Binding PositionBackground}" VerticalAlignment="Bottom" CornerRadius="0,0,4,4"/>
                            </Grid>
                            </Grid>
                          </Border>
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>
                  </StackPanel>
                  <StackPanel Visibility="{Binding IsRelative, Converter={StaticResource BoolToVisibility}}">
                    <ItemsControl ItemsSource="{Binding DataContext.RelativeDrivers, RelativeSource={RelativeSource AncestorType=Window}}">
                      <ItemsControl.ItemTemplate>
                        <DataTemplate>
                          <Border Height="27" BorderBrush="#263E464D" BorderThickness="0,0,0,1">
                            <Border.Style>
                              <Style TargetType="Border">
                                <Setter Property="Background" Value="#10000000" />
                                <Style.Triggers>
                                  <DataTrigger Binding="{Binding IsPlayer}" Value="True">
                                    <Setter Property="Background" Value="#274DE95F" />
                                    <Setter Property="BorderBrush" Value="#FF4DE95F" />
                                  </DataTrigger>
                                </Style.Triggers>
                              </Style>
                            </Border.Style>
                            <Grid>
                              <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="{Binding DataContext.PositionColumnWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
                                <ColumnDefinition Width="{Binding DataContext.CarNumberColumnWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
                                <ColumnDefinition Width="{Binding DataContext.DriverColumnWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
                                <ColumnDefinition Width="{Binding DataContext.LicenseColumnWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
                                <ColumnDefinition Width="{Binding DataContext.IRatingColumnWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
                                <ColumnDefinition Width="Auto" />
                              </Grid.ColumnDefinitions>
                              <Border Background="{Binding PositionBackground}" CornerRadius="3" Margin="1,4">
                                <TextBlock Text="{Binding Position}" Foreground="#FFF9FCFF" FontWeight="Bold" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Center" />
                              </Border>
                              <TextBlock Grid.Column="1" Text="{Binding CarNumber}" Style="{StaticResource WidgetNumber}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                              <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Width="19" Height="19" Background="Transparent" Margin="0,0,4,0" Visibility="{Binding DataContext.ShowLogos, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource BoolToVisibility}}"><Image Source="{Binding BrandImage}" Stretch="Uniform" Margin="1" SnapsToDevicePixels="True" RenderOptions.BitmapScalingMode="HighQuality"/></Border>
                                <Border Width="16" Height="12" Margin="0,0,4,0" BorderBrush="#405A6670" BorderThickness=".5" Visibility="{Binding DataContext.ShowFlags, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource BoolToVisibility}}">
                                  <Grid>
                                    <TextBlock Text="{Binding Flag}" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    <Image Source="{Binding FlagImage}" Stretch="Fill" RenderOptions.BitmapScalingMode="HighQuality" />
                                  </Grid>
                                </Border>
                                <TextBlock Text="{Binding Driver}" Style="{StaticResource WidgetValue}" FontSize="10" VerticalAlignment="Center" />
                              </StackPanel>
                              <Border Grid.Column="3" Background="#4A101A22" BorderBrush="{Binding LicenseBrush}" BorderThickness="1" CornerRadius="4" Padding="4,2" Margin="1,3"><TextBlock Text="{Binding License}" FontSize="9" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{Binding LicenseBrush}"/></Border>
                              <Border Grid.Column="4" Background="#FF222B33" BorderBrush="#FF77838E" BorderThickness="1" CornerRadius="4" Padding="3,0" Margin="1,2">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                  <TextBlock Text="{Binding IRatingValue}" FontSize="9.5" FontWeight="Bold" Foreground="#FFF4F7FA" VerticalAlignment="Center" />
                                  <iconPacks:PackIconMaterial Kind="{Binding IRatingArrow}" Width="9" Height="9" Foreground="{Binding IRatingDeltaBrush}" Margin="2,0,1,0" VerticalAlignment="Center" />
                                  <TextBlock Text="{Binding IRatingDelta}" FontSize="8.5" FontWeight="Bold" Foreground="{Binding IRatingDeltaBrush}" VerticalAlignment="Center" />
                                </StackPanel>
                              </Border>
                              <ItemsControl Grid.Column="5" ItemsSource="{Binding Fields}">
                                <ItemsControl.ItemsPanel>
                                  <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                  </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                  <DataTemplate>
                                    <Border Width="{Binding Width}" BorderBrush="{Binding Accent}" BorderThickness="0,0,0,1" Background="#12000000" Padding="2,0">
                                      <Grid VerticalAlignment="Center"><Grid.RowDefinitions><RowDefinition Height="14"/><RowDefinition Height="8"/></Grid.RowDefinitions><TextBlock Text="{Binding NonP2PValue}" Foreground="{Binding Accent}" FontWeight="Medium" FontSize="9.5" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.RowSpan="2"/><iconPacks:PackIconMaterial Kind="{Binding P2PIcon}" Width="8" Height="8" Foreground="{Binding Accent}" Margin="2,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" Visibility="{Binding IsP2P, Converter={StaticResource BoolToVisibility}}"/><TextBlock Text="{Binding P2PSeconds}" Foreground="{Binding Accent}" FontWeight="Bold" FontSize="9.5" HorizontalAlignment="Center" VerticalAlignment="Center"/><ProgressBar Grid.Row="1" Value="{Binding P2PLevel}" Maximum="100" Foreground="{Binding Accent}" Background="#35414A54" BorderThickness="0" Height="5" VerticalAlignment="Center" Visibility="{Binding IsP2P, Converter={StaticResource BoolToVisibility}}"/></Grid>
                                    </Border>
                                  </DataTemplate>
                                </ItemsControl.ItemTemplate>
                              </ItemsControl>
                            </Grid>
                          </Border>
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>
                  </StackPanel>
                  <Grid Visibility="{Binding IsFuel, Converter={StaticResource BoolToVisibility}}">
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <DockPanel Grid.ColumnSpan="2" Margin="0,0,0,5">
                      <StackPanel Orientation="Horizontal"><iconPacks:PackIconMaterial Kind="GasStationOutline" Width="14" Height="14" Foreground="#FF54D9E9" Margin="0,0,5,0"/><TextBlock Text="FUEL CALCULATOR" Style="{StaticResource Label}" /></StackPanel>
                      <TextBlock DockPanel.Dock="Right" Text="{Binding DataContext.FuelLapsText, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource Label}" Foreground="#FFF4F7FA" />
                    </DockPanel>
                    <StackPanel Grid.Row="1">
                      <TextBlock Text="{Binding DataContext.FuelLevelText, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource WidgetTitle}" FontSize="21" />
                      <Border Height="6" Background="#3335485C" CornerRadius="3" Margin="0,4,9,0"><Border Background="#FF54D9E9" Width="105" HorizontalAlignment="Left" CornerRadius="3" /></Border>
                      <TextBlock Text="{Binding DataContext.FuelAverageText, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource Label}" Margin="0,4,0,0" />
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="1">
                      <TextBlock Text="REFUEL TO FINISH" Style="{StaticResource Label}" />
                      <TextBlock Text="{Binding DataContext.FuelRefuelText, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource WidgetTitle}" FontSize="21" Foreground="#FFFFB84B" />
                      <TextBlock Text="BASED ON COMPLETED LAPS" Style="{StaticResource Label}" Foreground="#FFFFB84B" />
                    </StackPanel>
                    <Grid Grid.Row="2" Grid.ColumnSpan="2" Margin="0,7,0,0">
                      <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                      <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                      <StackPanel Visibility="{Binding ShowFuelLastLap, Converter={StaticResource BoolToVisibility}}"><TextBlock Text="LAST" Style="{StaticResource Label}"/><TextBlock Text="{Binding DataContext.FuelLastText, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource WidgetValue}"/></StackPanel>
                      <StackPanel Grid.Column="1" Visibility="{Binding ShowFuelFiveLap, Converter={StaticResource BoolToVisibility}}"><TextBlock Text="5 LAPS" Style="{StaticResource Label}"/><TextBlock Text="{Binding DataContext.FuelFiveText, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource WidgetValue}"/></StackPanel>
                      <StackPanel Grid.Column="2" Visibility="{Binding ShowFuelMax, Converter={StaticResource BoolToVisibility}}"><TextBlock Text="MAX" Style="{StaticResource Label}"/><TextBlock Text="{Binding DataContext.FuelMaxText, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource WidgetValue}"/></StackPanel>
                      <StackPanel Grid.Column="3" Visibility="{Binding ShowFuelLaps, Converter={StaticResource BoolToVisibility}}"><TextBlock Text="IN TANK" Style="{StaticResource Label}"/><TextBlock Text="{Binding DataContext.FuelLapsText, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource WidgetValue}" Foreground="#FF54D9E9"/></StackPanel>
                      <Border Grid.Row="1" Grid.ColumnSpan="4" Background="#9B0A1018" BorderBrush="#4254D9E9" BorderThickness="0,1,0,0" CornerRadius="3" Padding="0,4,0,0" Margin="0,5,0,0"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="{Binding DataContext.FuelPitByLapText, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource WidgetValue}" Foreground="#FFF4F7FA"/><TextBlock Grid.Column="1" Text="{Binding DataContext.FuelPitAddText, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource WidgetValue}" Foreground="#FFFFB84B" Margin="8,0"/><TextBlock Grid.Column="2" Text="{Binding DataContext.FuelPitStopsText, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource Label}" Foreground="#FF54D9E9" VerticalAlignment="Center"/></Grid></Border>
                    </Grid>
                  </Grid>
                  <StackPanel Visibility="{Binding IsWeather, Converter={StaticResource BoolToVisibility}}">
                    <DockPanel>
                      <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="WeatherPartlyCloudy" Width="15" Height="15" Foreground="#FF69DDEC" Margin="0,0,5,0" />
                        <TextBlock Text="CONDIÇÕES DA PISTA" Style="{StaticResource Label}" />
                      </StackPanel>
                    </DockPanel>
                    <Grid Margin="0,8,0,0"><Grid.RowDefinitions><RowDefinition/><RowDefinition/></Grid.RowDefinitions><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                      <StackPanel><TextBlock Text="CLIMA" Style="{StaticResource Label}"/><TextBlock Text="{Binding DataContext.WeatherClimateText, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" FontSize="12"/></StackPanel>
                      <StackPanel Grid.Column="1"><TextBlock Text="PISTA" Style="{StaticResource Label}"/><TextBlock Text="{Binding DataContext.WeatherTemperatureText, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" FontSize="12" Foreground="#FF69DDEC"/></StackPanel>
                      <StackPanel Grid.Row="1" Margin="0,8,0,0"><TextBlock Text="TRACK WETNESS" Style="{StaticResource Label}"/><TextBlock Text="{Binding DataContext.WeatherRainText, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" FontSize="12"/></StackPanel>
                      <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,8,0,0"><TextBlock Text="GRIP" Style="{StaticResource Label}"/><TextBlock Text="{Binding DataContext.WeatherGripText, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" FontSize="12" Foreground="#FF4DE95F"/></StackPanel>
                    </Grid>
                  </StackPanel>
                  <Grid Visibility="{Binding IsRadar, Converter={StaticResource BoolToVisibility}}" HorizontalAlignment="Center">
                    <Ellipse Width="148" Height="148" Stroke="#706AD6EF" StrokeThickness="1" />
                    <Ellipse Width="96" Height="96" Stroke="#306AD6EF" StrokeThickness="1" />
                    <Line X1="74" Y1="4" X2="74" Y2="144" Stroke="#305A718C" />
                    <Line X1="4" Y1="74" X2="144" Y2="74" Stroke="#305A718C" />
                    <ItemsControl ItemsSource="{Binding DataContext.RadarDots, RelativeSource={RelativeSource AncestorType=Window}}" IsHitTestVisible="False">
                      <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                          <Canvas Width="148" Height="148" />
                        </ItemsPanelTemplate>
                      </ItemsControl.ItemsPanel>
                      <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                          <Setter Property="Canvas.Left" Value="{Binding Left}" />
                          <Setter Property="Canvas.Top" Value="{Binding Top}" />
                        </Style>
                      </ItemsControl.ItemContainerStyle>
                      <ItemsControl.ItemTemplate>
                        <DataTemplate>
                          <StackPanel HorizontalAlignment="Center">
                            <StackPanel.ToolTip>
                              <TextBlock Text="{Binding Label}" />
                            </StackPanel.ToolTip>
                            <Border Width="14" Height="14" Background="{Binding Fill}" CornerRadius="7" BorderBrush="#FF07101D" BorderThickness="2" HorizontalAlignment="Center" />
                            <Border Background="#D60B101D" CornerRadius="2" Padding="2,0" Margin="0,1,0,0" HorizontalAlignment="Center">
                              <TextBlock Text="{Binding DistanceLabel}" FontSize="8" FontWeight="Bold" Foreground="#FFE7EDF5" />
                            </Border>
                          </StackPanel>
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <Border Width="29" Height="43" Background="#FF55DDF5" CornerRadius="14" HorizontalAlignment="Center" VerticalAlignment="Center">
                      <TextBlock Text="VOCÊ" Foreground="#FF07101D" FontSize="7" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Border>
                    <Border Background="#D60B101D" CornerRadius="3" Padding="4,2" HorizontalAlignment="Right" VerticalAlignment="Top">
                      <TextBlock Text="{Binding DataContext.RadarDistanceText, RelativeSource={RelativeSource AncestorType=Window}}" FontSize="9" />
                    </Border>
                    <TextBlock Text="{Binding DataContext.RadarSideText, RelativeSource={RelativeSource AncestorType=Window}}" VerticalAlignment="Bottom" HorizontalAlignment="Center" Foreground="#FFB5C2D4" FontSize="9" />
                  </Grid>
                  <StackPanel Visibility="{Binding IsStartHelper, Converter={StaticResource BoolToVisibility}}">
                    <DockPanel>
                      <TextBlock Text="START HELPER" FontWeight="Bold" FontSize="11" />
                      <TextBlock DockPanel.Dock="Right" Text="60 FPS" Style="{StaticResource Label}" />
                    </DockPanel>
                    <Grid Margin="0,9,0,0">
                      <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                      </Grid.RowDefinitions>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="78" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="42" />
                      </Grid.ColumnDefinitions>
                      <TextBlock Text="EMBREAGEM" Style="{StaticResource Label}" />
                      <ProgressBar Grid.Column="1" Value="{Binding DataContext.ClutchPct, RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay}" Maximum="100" Height="11" />
                      <TextBlock Grid.Column="2" Text="{Binding DataContext.ClutchText, RelativeSource={RelativeSource AncestorType=Window}}" HorizontalAlignment="Right" FontWeight="Bold" />
                      <TextBlock Grid.Row="1" Text="ACELERADOR" Style="{StaticResource Label}" />
                      <ProgressBar Grid.Row="1" Grid.Column="1" Value="{Binding DataContext.ThrottlePct, RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay}" Maximum="100" Height="11" />
                      <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding DataContext.ThrottleText, RelativeSource={RelativeSource AncestorType=Window}}" HorizontalAlignment="Right" FontWeight="Bold" />
                    </Grid>
                  </StackPanel>
                </Grid>
              </Grid>
              <Border Width="10" Height="10" Background="#B8FFFFFF" BorderBrush="#55FFFFFF" BorderThickness="1" CornerRadius="5" Cursor="SizeNWSE" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,5,5" PreviewMouseLeftButtonDown="OnResizeMouseDown" PreviewMouseMove="OnResizeMouseMove" PreviewMouseLeftButtonUp="OnResizeMouseUp" Visibility="{Binding DataContext.IsEditing, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVisibility}}" />
            </Grid>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </Grid>
</Window>
