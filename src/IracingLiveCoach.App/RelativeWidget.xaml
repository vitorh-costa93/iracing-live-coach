<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:IracingLiveCoach.App" x:Class="IracingLiveCoach.App.RelativeWidget" x:Name="Root" Title="Relative" WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ShowInTaskbar="False" MinWidth="600" MinHeight="320" SizeToContent="Manual" UseLayoutRounding="True" SnapsToDevicePixels="True" TextOptions.TextFormattingMode="Display">
    <Grid Background="Transparent" MouseLeftButtonDown="OnBackgroundMouseLeftButtonDown">
        <Border x:Name="OuterBorder" local:BroadcastChrome.Enabled="True" Background="{StaticResource F1BackgroundBrush}" BorderBrush="{StaticResource F1BorderIdleBrush}" BorderThickness="1" RenderOptions.BitmapScalingMode="HighQuality">
            <Grid Margin="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="4"/>
                    <RowDefinition Height="0"/>
                    <RowDefinition Height="0"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="88"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="{StaticResource F1AccentBrush}"/>
                <Border Grid.Row="3" BorderBrush="{StaticResource F1SeparatorBrush}" BorderThickness="0,0,0,1">
                    <Grid VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{Binding ElementName=Root, Path=DataContext.PosColumnWidth, Mode=TwoWay}"/>
                            <ColumnDefinition Width="*" MinWidth="158"/>
                            <ColumnDefinition Width="{Binding ElementName=Root, Path=DataContext.LicColumnWidth, Mode=TwoWay}"/>
                            <ColumnDefinition Width="{Binding ElementName=Root, Path=DataContext.IRatingColumnWidth, Mode=TwoWay}"/>
                            <ColumnDefinition Width="{Binding ElementName=Root, Path=DataContext.DeltaColumnWidth, Mode=TwoWay}"/>
                            <ColumnDefinition Width="{Binding ElementName=Root, Path=DataContext.OtColumnWidth, Mode=TwoWay}"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="POS" Style="{StaticResource HeaderCell}"/>
                        <TextBlock Grid.Column="1" Text="PILOTO" Style="{StaticResource HeaderCell}"/>
                        <TextBlock Grid.Column="2" Text="LIC." Style="{StaticResource HeaderCell}"/>
                        <TextBlock Grid.Column="3" Text="iR" Style="{StaticResource HeaderCell}"/>
                        <TextBlock Grid.Column="4" Text="DELTA" Style="{StaticResource HeaderCell}"/>
                        <TextBlock Grid.Column="5" Text="OT / ESTADO" Style="{StaticResource HeaderCell}" Visibility="{Binding HasOvertake, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        <GridSplitter Grid.Column="1" Width="6" Margin="-3,0,0,0" HorizontalAlignment="Left" Background="Transparent" ResizeBehavior="PreviousAndNext" ToolTip="Arraste para ajustar a coluna" Cursor="SizeWE"/>
                        <GridSplitter Grid.Column="2" Width="6" Margin="-3,0,0,0" HorizontalAlignment="Left" Background="Transparent" ResizeBehavior="PreviousAndNext" ToolTip="Arraste para ajustar a coluna" Cursor="SizeWE"/>
                        <GridSplitter Grid.Column="3" Width="6" Margin="-3,0,0,0" HorizontalAlignment="Left" Background="Transparent" ResizeBehavior="PreviousAndNext" ToolTip="Arraste para ajustar a coluna" Cursor="SizeWE"/>
                        <GridSplitter Grid.Column="4" Width="6" Margin="-3,0,0,0" HorizontalAlignment="Left" Background="Transparent" ResizeBehavior="PreviousAndNext" ToolTip="Arraste para ajustar a coluna" Cursor="SizeWE"/>
                        <GridSplitter Grid.Column="5" Width="6" Margin="-3,0,0,0" HorizontalAlignment="Left" Background="Transparent" ResizeBehavior="PreviousAndNext" ToolTip="Arraste para ajustar a coluna" Cursor="SizeWE"/>
                    </Grid>
                </Border>
                <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Rows}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Height="54" BorderBrush="{StaticResource F1SeparatorBrush}" BorderThickness="0,0,0,1">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsPlayerRow}" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource F1HighlightBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="{Binding ElementName=Root, Path=DataContext.PosColumnWidth, Mode=TwoWay}"/>
                                            <ColumnDefinition Width="*" MinWidth="158"/>
                                            <ColumnDefinition Width="{Binding ElementName=Root, Path=DataContext.LicColumnWidth, Mode=TwoWay}"/>
                                            <ColumnDefinition Width="{Binding ElementName=Root, Path=DataContext.IRatingColumnWidth, Mode=TwoWay}"/>
                                            <ColumnDefinition Width="{Binding ElementName=Root, Path=DataContext.DeltaColumnWidth, Mode=TwoWay}"/>
                                            <ColumnDefinition Width="{Binding ElementName=Root, Path=DataContext.OtColumnWidth, Mode=TwoWay}"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Width="4" Background="{StaticResource F1AccentBrush}" HorizontalAlignment="Left"/>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding PositionText}" FontFamily="{StaticResource F1MonoFont}" FontSize="16" FontWeight="SemiBold" Foreground="{Binding RowForegroundBrush}"/>
                                        </StackPanel>
                                        <Grid Grid.Column="1" VerticalAlignment="Center">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*" MinWidth="158"/>
                                            </Grid.ColumnDefinitions>
                                            <Border Width="18" Height="12" BorderBrush="#B0FFFFFF" BorderThickness="1" Margin="0,0,5,0">
                                                <Image Source="{Binding FlagImagePath}" Stretch="Fill"/>
                                            </Border>
                                            <Border Grid.Column="1" Width="28" Height="28" CornerRadius="0" Background="Transparent" Margin="0,0,8,0" Visibility="{Binding HasBrandIcon, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <Grid>
                                                    <Image Source="{Binding BrandImagePath}" Width="26" Height="26" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality" Visibility="{Binding HasBrandImage, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                    <Viewbox Width="11" Height="11" Visibility="{Binding HasBrandPath, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <Canvas Width="24" Height="24">
                                                            <Path Data="{Binding BrandPathData}" Fill="{Binding BrandColorBrush}"/>
                                                        </Canvas>
                                                    </Viewbox>
                                                </Grid>
                                            </Border>
                                            <TextBlock Grid.Column="2" Text="{Binding FlagAndCode}" FontFamily="{StaticResource F1MonoFont}" FontSize="14" FontWeight="SemiBold" Foreground="{Binding RowForegroundBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                        </Grid>
                                        <Border Grid.Column="2" Background="{Binding LicBrush}" Padding="5,3" HorizontalAlignment="Left" VerticalAlignment="Center" CornerRadius="3" BorderBrush="#50FFFFFF" BorderThickness="1">
                                            <TextBlock Text="{Binding LicText}" FontFamily="{StaticResource F1MonoFont}" FontSize="12" FontWeight="SemiBold" Foreground="White"/>
                                        </Border>
                                        <TextBlock Grid.Column="3" Text="{Binding IRatingText}" FontFamily="{StaticResource F1DataFont}" FontSize="13" Foreground="{Binding RowForegroundBrush}" VerticalAlignment="Center" Margin="4,0"/>
                                        <TextBlock Grid.Column="4" Text="{Binding GapText}" FontFamily="{StaticResource F1DataFont}" FontSize="13" FontWeight="SemiBold" Foreground="{Binding RowForegroundBrush}" VerticalAlignment="Center" Margin="4,0"/>
                                        <Border Grid.Column="5" Margin="3,4,5,4" Padding="5,2" CornerRadius="3" Background="#FF101920" BorderThickness="1" BorderBrush="{Binding P2PBrush}" Visibility="{Binding HasP2P, Converter={StaticResource BoolToVisibilityConverter}}" ToolTip="{Binding P2PText}">
                                            <StackPanel>
                                                <TextBlock Text="{Binding P2PStateText}" FontFamily="{StaticResource F1MonoFont}" FontSize="11" FontWeight="Bold" Foreground="{Binding P2PBrush}"/>
                                                <TextBlock Text="{Binding P2PDetailText}" FontFamily="{StaticResource F1DataFont}" FontSize="10" Foreground="White"/>
                                            </StackPanel>
                                        </Border>
                                        <Border Width="4" Background="{Binding ClassAccentBrush}" HorizontalAlignment="Left" ToolTip="{Binding ClassBadgeText}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                <Grid Grid.Row="5" VerticalAlignment="Center">
                    <TextBlock Text="- À FRENTE    + ATRÁS" FontFamily="{StaticResource F1MonoFont}" FontSize="13" Foreground="{StaticResource F1TextBrush}"/>
                    <TextBlock Text="✓ PRONTO  •  ϟ ATIVO  •  ◷ RECARGA" HorizontalAlignment="Right" FontFamily="{StaticResource F1MonoFont}" FontSize="9" Foreground="{StaticResource F1PositionGainBrush}" Visibility="{Binding HasOvertake, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Grid>
                <Border Grid.Row="6" BorderBrush="{StaticResource F1SeparatorBrush}" BorderThickness="0,1,0,1">
                    <Grid Margin="12,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="BRAKE BIAS" Style="{StaticResource FooterLabel}"/>
                            <TextBlock Text="{Binding BrakeBiasText}" Style="{StaticResource FooterValue}" FontSize="30"/>
                            <TextBlock Text="DIANTEIRO" Style="{StaticResource FooterLabel}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="TEMP. PISTA" Style="{StaticResource FooterLabel}"/>
                            <TextBlock Text="{Binding TrackTempText}" Style="{StaticResource FooterValue}" FontSize="30"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="EMBORRACHAMENTO" Style="{StaticResource FooterLabel}"/>
                            <TextBlock Text="{Binding TrackRubberText}" FontFamily="{StaticResource F1MonoFont}" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource F1TextBrush}"/>
                            <TextBlock Text="{Binding RubberBlocksText}" FontSize="15" Foreground="#FFFFAE00"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
        <Thumb x:Name="ResizeGrip" Width="14" Height="14" Cursor="SizeNWSE" HorizontalAlignment="Right" VerticalAlignment="Bottom" Background="{StaticResource F1AccentBrush}" Opacity="0.8" DragDelta="OnResizeGripDragDelta" DragCompleted="OnResizeGripDragCompleted"/>
    </Grid>
</Window>
