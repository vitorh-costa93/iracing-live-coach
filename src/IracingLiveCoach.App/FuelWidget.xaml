<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:IracingLiveCoach.App" x:Class="IracingLiveCoach.App.FuelWidget" Title="Fuel" WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ShowInTaskbar="False" MinWidth="270" MinHeight="240" SizeToContent="Manual" UseLayoutRounding="True">
    <Grid Background="Transparent" MouseLeftButtonDown="OnBackgroundMouseLeftButtonDown">
        <Border x:Name="OuterBorder" local:BroadcastChrome.Enabled="True" Background="{StaticResource F1BackgroundBrush}" BorderBrush="{StaticResource F1BorderIdleBrush}" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <Border Height="4" Background="{StaticResource F1AccentBrush}"/>
                    <Border Padding="16,10" BorderBrush="{StaticResource F1SeparatorBrush}" BorderThickness="0,0,0,1" Visibility="{Binding ShowLevel, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel>
                            <TextBlock Text="NO TANQUE" Style="{StaticResource FooterLabel}"/>
                            <TextBlock Text="{Binding FuelLevelText}" Style="{StaticResource FooterValue}" FontSize="42" Foreground="White" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Padding="16,10" BorderBrush="{StaticResource F1SeparatorBrush}" BorderThickness="0,0,0,1" Visibility="{Binding ShowAveragePerLap, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel>
                            <TextBlock Text="CONSUMO MÉDIO / VOLTA" Style="{StaticResource FooterLabel}"/>
                            <TextBlock Text="{Binding AveragePerLapText}" Style="{StaticResource FooterValue}" FontSize="25" Foreground="White" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Padding="16,10" BorderBrush="{StaticResource F1SeparatorBrush}" BorderThickness="0,0,0,1" Visibility="{Binding ShowUsePerHour, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel>
                            <TextBlock Text="CONSUMO POR HORA" Style="{StaticResource FooterLabel}"/>
                            <TextBlock Text="{Binding FuelUsePerHourText}" Style="{StaticResource FooterValue}" FontSize="25" Foreground="White" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Padding="16,10" BorderBrush="{StaticResource F1SeparatorBrush}" BorderThickness="0,0,0,1" Visibility="{Binding ShowLapsRemaining, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel>
                            <TextBlock Text="AUTONOMIA / VOLTAS" Style="{StaticResource FooterLabel}"/>
                            <TextBlock Text="{Binding LapsRemainingText}" Style="{StaticResource FooterValue}" FontSize="32" Foreground="{StaticResource F1PositionGainBrush}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Padding="16,10" BorderBrush="{StaticResource F1SeparatorBrush}" BorderThickness="0,0,0,1" Visibility="{Binding ShowTimeRemaining, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel>
                            <TextBlock Text="AUTONOMIA / TEMPO" Style="{StaticResource FooterLabel}"/>
                            <TextBlock Text="{Binding TimeRemainingText}" Style="{StaticResource FooterValue}" FontSize="32" Foreground="{StaticResource F1PositionGainBrush}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>
        <Thumb x:Name="ResizeGrip" Width="12" Height="12" Cursor="SizeNWSE" HorizontalAlignment="Right" VerticalAlignment="Bottom" Background="{StaticResource F1AccentBrush}" DragDelta="OnResizeGripDragDelta" DragCompleted="OnResizeGripDragCompleted"/>
    </Grid>
</Window>
